# axx ミニ言語 構文リファレンス

`binary_list` 欄から `.call` で呼ばれる、チューリング完全な小さな手続き型言語。
パターンファイル（`.axx`）の中だけで使う。

マクロ層とは別の段である。マクロ層がファイルを読み込む前にテキストを変換するのに
対し、ミニ言語は符号化の時点、すなわち命令 1 行ごとに走る。呼ばれるのは
`binary_list` に `.call` と書いた行だけで、書かなければ従来どおりパターンファイルは
宣言的なデータのままである。

axx.py と caxx.c の両方に同一仕様で実装されている。値は両実装とも 256bit 2 の補数で、
同じ入力に同じバイト列を出す。`.echo` が標準エラーへ書く文字列も両実装で一致する。

## 定義と呼び出し

```
.func <名前>(<引数, 引数, ...>)
<文>
.endfunc
```

見出しから対応する `.endfunc` までが本体で、この行はパターン行としては照合されない。
引数が無いときは `.func name()` でも `.func name` でもよい。名前と引数名は英数字と
`_`。呼び出し側の `.call name(a,b)` と同じ書き方である。

旧来の `.func::名前::引数,引数` も読める。`.func` の直後が `::` のときだけそちらに
切り替わるので、既存のパターンファイルはそのまま動く。

```
MOV a,!b,!c :: .call name(a,b,c)
```

`.call` はカンマ区切りの 1 要素なので、他の値と混ぜて書ける。

```
MIX !e :: 0x90,.call rep(e),0xff
```

引数に `[式, 式, ...]` と書くと配列を渡せる。要素もパターン層の式。`[]` は空の配列。

```
LOG !v :: .call table([0x11,0x22,v],3)
```

関数は使う場所より後ろで定義してよい。パターンファイルを全部読んでから解決する。

## 文

1 行 1 文。

| 構文 | 意味 |
|---|---|
| `name = 式` | ローカル変数への代入 |
| `name[添字] = 式` | 配列要素への代入 |
| `.emit(e1, e2, ...)` | 値 1 つにつき 1 ワード出力する |
| `.echo(項目, 項目, ...)` | 文字列と値を標準エラーへ表示する。ワードは出さない |
| `.raise 式` | 式の値をエラーコードとしてエラーを報告する |
| `.call 名前(引数, ...)` | 他の関数を呼ぶ |
| `name = .call 名前(引数, ...)` | 他の関数を呼び、返り値を代入する |
| `.if 式 .then` / `.elif 式 .then` / `.else` / `.endif` | 条件分岐。`.elif` は何段でも書け、`.else` は省略可 |
| `.while(式)` / `.endwhile` | 式が 0 でない間くり返す |
| `.for 名前 in range(...)` / `.next` | `range(stop)` `range(start, stop)` `range(start, stop, step)` |
| `.break` | いちばん内側の `.while` / `.for` を抜ける |
| `.continue` | いちばん内側の `.while` / `.for` の次のくり返しへ進む |
| `.nonlocal a, b` | その名前を外側の呼び出しのものとして扱う |
| `.return` | 関数から戻る。本体中どこでも（トップレベルでも `.if`/`.while`/`.for` の中でも）、何度でも書ける |
| `.return 式` | 値を返して関数から戻る |
| `.func 名前(引数...)` … `.endfunc` | 入れ子の関数定義。本体を閉じるのは `.endfunc` |

`.if` と `.elif` の行は `.then` で終わること。`.if` のあとに `.elif` を何段
続けてもよく、最後に `.else` を置ける。連鎖全体を閉じる `.endif` は 1 つでよい。
`.while` の条件は括弧の有無を問わない（`(式)` は括弧式として読める）。
`range()` の `step` に 0 は書けない。
`.break` と `.continue` は `.while` か `.for` の中だけに書ける。ループの外に
書くとその場でエラーになる。どちらもいちばん内側のループ 1 段にだけ効く。

```
.for i in range(10)
.if i == 3 .then
.continue          /* 3 は飛ばして次へ */
.endif
.if i == 7 .then
.break             /* 7 で打ち切る */
.endif
.emit(i)
.next
```

```
.if n > 100 .then
.emit(0xff, n & 0xff)
.elif n > 10 .then
.emit(0xfe, n & 0xff)
.else
.emit(n & 0xff)
.endif
```

`.echo` はデバッグ用である。標準エラーへ表示するだけでワードを出さないので、
足しても消しても生成されるバイト列は変わらない。項目は `"..."` の文字列
リテラルか式で、混ぜて書ける。文字列はそのまま、整数は符号つき 10 進、配列は
`[1, 2, 3]` の形で表示し、1 回の `.echo` の項目は空白区切りで 1 行にまとめる。
体裁はマクロ層の `!echo` と共通の出力ルーチンで決めているので、両者で揃う。
`.echo()` と書けば空行を出す。命令長を測るだけの試し打ちと、収束途中のパス1
では何も出さないので、組み立てた命令 1 つにつき 1 行だけ出る。

```
.echo("Example", n, 1)          /* -> Example 7 1 */
.echo("n=", n, "arr=", a)       /* -> n= 7 arr= [7, 14] */
```

文字列が書けるのは `.echo` の引数欄だけである。値は整数と配列だけなので、
変数へ代入したり式の中で使ったりはできない。逃げ記号は `\\` `\"` `\n` `\t`
の 4 つで、それ以外の `\` はエラーになる。文字列の中に `/*` は書けない。
パターンファイルのコメント記号として、ミニ言語が読むより前に落とされるためである。

## エラーの報告

`.raise 式` は、式の値をエラーコードとしてエラーを報告する。`error_patterns` 欄に
`条件;コード` と書いたときとまったく同じ体裁で、`.error::コード::"文言"` に登録した
文言も同じように使われる。

```
.error::3::"immediate out of range"

MOV !n :: .call mov(n)

.func mov(n)
.if n > 255 .then
.raise 3
.return
.endif
.emit(0xb0, n)
.return
.endfunc
```

```
MOV 300              -> Line 1 Error code 3 immediate out of range:
```

- **報告しても関数は止まらない。** `.raise` の次の文から続けて走るので、そこで
  打ち切りたいなら上の例のように `.return` を続けて書く。
- **登録していないコードでもよい。** その場合は文言なしで
  `Line 1 Error code 9 : ` のように番号だけが出る。
- **`.echo` と同じく、命令長を測るだけの試し打ちと収束途中のパス1では黙る。**
  組み立てた命令 1 つにつき 1 回だけ報告される。
- **アセンブルは失敗になる。** `error_patterns` が引っかかったときと同じく、
  出力ファイルは書かれない。

## 本体の式評価への委譲

式の中に次のものを書くと、その項はアセンブラ本体の式評価器が評価する。
値はどちらも 256bit なのでそのまま使える。

| 書き方 | 意味 |
|---|---|
| `$$` | 位置カウンタ（この命令の先頭アドレス） |
| `$.` | 次の命令の先頭アドレス |
| `#名前` | `.setsym` で定義した記号 |
| `名前` | ローカル変数に無ければアセンブラのラベル / `.equ` |

```
BR !t :: .call rel8(t)

.func rel8(target)
d = target - $.
.if d < -128 || d > 127 .then
.raise 2
.endif
.emit(d & 0xff)
.return
.endfunc
```

本体の評価器は呼ばれた文脈ごとに使える項目が変わる（能力記述子）。ミニ言語から
呼ぶときは次のようになる。

| 項目 | パターン行 | ミニ言語 |
|---|---|---|
| ラベル・`.equ` | 使える | 使える |
| `$$` / `$.` | 使える | 使える |
| `#記号` | 使える | 使える |
| パターン変数 `a`〜`z` | 使える | **使えない** |
| `!!!` / `!!!!` | 使える | **使えない** |

パターン変数は `.func` の本体が走っている時点では束縛されていないので落とす。
引数として渡したいときは `.call f(a,b)` のように呼び出し側で渡す。`!!!` は
VLIW のパターン行でしか意味を持たないので同様に落とす。

未定義ラベル由来の値は 0 になる。`.call` の引数と同じ扱いで、番兵の巨大な値で
反復回数が爆発しないようにするためである。綴り間違いは、本体の表が揃うパス2で
「そんな名前は無い」と判定できるので、従来どおりミニ言語のエラーになる。

## 式と演算子

優先順位は低いほうから。

| 優先 | 演算子 |
|---|---|
| 1 | `\|\|` |
| 2 | `&&` |
| 3 | `!`（前置） |
| 4 | `==` `!=` `<` `<=` `>` `>=` |
| 5 | `\|` |
| 6 | `^` |
| 7 | `&` |
| 8 | `<<` `>>` |
| 9 | `+` `-` |
| 10 | `*` `/` `%` |
| 11 | `-` `+` `~`（前置） |
| 12 | `**`（右結合） |
| 13 | `[添字]` `[開始:終了]` |

- `/` は 0 方向への切り捨て、`%` の符号は被除数に従う（`-7/2` は `-3`、`-7%3` は `-1`）。
- `>>` は算術シフト。シフト量が 256 以上なら `<<` は 0、`>>` は符号で 0 か `-1`。
- `**` の指数に負数は書けない。結果は 256bit に収まる形で回る。
- 比較と論理演算は 1 か 0 を返す。`&&` `||` は短絡する。
- 数値は 10 進、`0x` 16 進、`0b` 2 進。桁区切りに `_` を書ける。
- `.len(式)` は配列の長さ。

条件として偽になるのは 0 だけである。

## 値と配列

値は整数か配列。配列の要素は整数で、配列の配列は作れない。

```
a = []
a[3] = 5          /* a は [0,0,0,5]。間は 0 で埋まる */
.emit(a[0])       /* 0 */
.emit(a[99])      /* 0。範囲外の読みは 0 で、配列は伸びない */
.emit(.len(a))    /* 4 */
b = a[1:3]        /* [0,0]。終了の添字は含まない */
```

| 操作 | ふるまい |
|---|---|
| `a[i] = v`（`i` が長さ以上） | 0 で埋めて `i+1` の長さまで伸ばす |
| `a[i]`（範囲外・負） | `0` を返す。配列は変わらない |
| `a[i] = v`（`i` が負） | エラー |
| `a[lo:hi]` | 配列の範囲に切り詰める。`hi` は含まない |
| `.len(a)` | 長さ |

代入前の名前を読むとエラーになる。綴り間違いが黙って 0 にならないようにするため。

## スコープと入れ子

呼び出しごとに変数一式ができる。引数はその呼び出しのローカルである。

関数は関数の中に定義できる。内側の関数は外側から見え、名前は自分 → 親 → …
→ トップレベルの順に外へたどって解決される。`.nonlocal` を書くと、その名前は
自分のローカルではなく外側の呼び出しの変数を指す。

```
.func outer()
n=7
.func inner()
.nonlocal n
n=n+1
.emit(n)
.return
.endfunc
.call inner()
.call inner()
.emit(n)
.return
.endfunc
```

```
-> 08 09 09
```

`.nonlocal` は、その関数の中でその名前を使うより前に書くこと。外側にその名前が
無ければエラーになる。

## 返り値

`.return 式` と書くと値を返す。呼ぶ側は `var = .call 名前(引数, ...)` の形で受け取る。

```
.func hypot2(a,b)
.return a*a+b*b
.endfunc

.func emit_h(a,b)
v = .call hypot2(a,b)
.emit(v)
.return
.endfunc
```

- **値は整数でも配列でもよい。** 配列はコピーして渡るので、受け取った側で書き換えても
  呼ばれた側の変数には影響しない。
- **受け取り先は配列要素でもよい。** `a[i] = .call f(x)` と書ける。この場合、
  返り値は整数であること。
- **値を返さない関数を `var = .call ...` で呼ぶとエラー。** 値のない `.return` で
  戻った場合と、`.return` を踏まずに本体を終えた場合の両方が該当する。
- **`.call` を式の途中には書けない。** `x = 1 + .call f(y)` は書けないので、
  いったん変数で受けてから使う。
- **`.return` は本体を閉じない。** 本体を閉じるのは常に `.endfunc` で、`.return` /
  `.return 式` はトップレベルでも `.if`/`.while`/`.for` の中でも、何度でも書ける
  早期リターン文である。
- **`binary_list` から呼んだ関数の返り値はそのまま出力になる。** スカラーなら
  1 ワード、配列なら添字 0 から順に 1 要素 1 ワード。`.emit` した値の後ろに続く
  ので、`.emit` だけで値を返さない関数は従来どおりのふるまいになる。

```
SEQ !n :: 0xaa,.call seq(n),0xbb

.func seq(n)
a=[]
.for i in range(n)
a[i]=0xc0+i
.next
.return a
.endfunc
```

```
seq 4                -> aa c0 c1 c2 c3 bb
```

```
.func mkarr(n)
a=[]
.for i in range(n)
a[i]=i*i
.next
.return a
.endfunc

.func use(n)
v = .call mkarr(n)
.emit(.len(v))
.emit(v[2])
.return
.endfunc
```

早期に戻ることもできる。

```
.func firstdiv(n)
i=2
.while(i<n)
.if n%i==0 .then
.return i
.endif
i=i+1
.endwhile
.return 0
.endfunc
```

## パターン層との境界

- **引数はパターン層の式。** `.call f(a,b)` の `a` `b` は捕捉済みのパターン変数を
  指す。ラベル、`.equ`、`$.` など、`binary_list` に書けるものはすべて書ける。
  ラベルの前方参照も使える。`[式, 式, ...]` と書けば配列を渡せる。
- **変数の名前空間は別。** ミニ言語の変数はパターン変数 `a`〜`z` とも `.setsym` の
  シンボルとも無関係で、渡したいものは引数にする。
- **未定義ラベル由来の引数は 0 になる。** パス 1 で命令長を測るときに、番兵の巨大な
  値で反復回数が爆発しないようにするため。
- **`.emit` は `.bits` 幅で 1 ワード。** 既定の 8bit なら 1 バイト、`.bits::12` なら
  12bit ワードを 1 個出す。
- **出力ワード数がその命令の長さになる。** 両パスで同じ個数を出すこと。パスによって
  個数が変わる書き方をすると、アドレスが収束しない。
- **`;` は `.call` にも付けられる。** `;.call f(a)` は、その呼び出しが出す値が
  1 ワードで 0 のときだけ何も出さない。あってもなくてもよいプレフィックスバイトは
  この形で書く。`;;.call f(a)` は呼んだうえで出力を捨てる。
- **`;;要素` は何も出力しない。** `binary_list` の要素の前に `;;` を書くと、
  評価はするがワードは出さない。`;;n` だけの `binary_list` は長さ 0 になる。

## 暴走ガード

チューリング完全なので、パターンファイルの誤りでアセンブラが止まらなくなりうる。
次の上限を超えると、原因の行を名指ししてエラーになる。

| 上限 | 値 |
|---|---|
| `.call` 1 回あたりの実行文数 | 4,000,000 |
| 呼び出しのネスト | 128 |
| `.call` 1 回あたりの出力ワード数 | 1,048,576 |
| 配列の長さ | 1,048,576 |

構文の誤りはパターンファイルを読んだ時点で、実行時の誤りはその命令を組み立てた
時点で、いずれもファイル名と行番号つきで報告される。

## 例

篩と、コラッツ列の長さを符号化する命令。

```
PRIMES !n  :: .call sieve(n)
COLLATZ !n :: .call collatz(n)

.func sieve(n)
mark=[]
mark[n]=0
i=2
.while(i*i<n)
.if mark[i]==0 .then
j=i*i
.while(j<n)
mark[j]=1
j=j+i
.endwhile
.endif
i=i+1
.endwhile
.for k in range(2,n)
.if mark[k]==0 .then
.emit(k)
.endif
.next
.return
.endfunc

.func collatz(n)
c=0
.while(n!=1)
.if n%2==0 .then
n=n/2
.else
n=3*n+1
.endif
c=c+1
.endwhile
.emit(c)
.return
.endfunc
```

```
primes 50            -> 02 03 05 07 0b 0d 11 13 17 1d 1f 25 29 2b 2f
collatz 27           -> 6f
```

再帰も使える。

```
FIB !n :: .call fib(n)

.func fib(k)
.if k<2 .then
.emit(k)
.else
.call fib(k-1)
.call fib(k-2)
.endif
.return
.endfunc
```

返り値を使えば、同じ再帰で値そのものを組み立てられる。

```
FIBV !n :: .call fibv(n)

.func fibn(k)
.if k<2 .then
.return k
.endif
p = .call fibn(k-1)
q = .call fibn(k-2)
.return p+q
.endfunc

.func fibv(n)
v = .call fibn(n)
.emit(v)
.return
.endfunc
```

```
fibv 10              -> 37
```
