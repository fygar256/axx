# axx マクロ層 構文リファレンス

アセンブラ本体に渡す前に走るソース→ソース変換段。ラベル値・`.equ`・`$`/`$$` は
参照できない（relaxation の各パスで展開結果が変わらないことを保証するため）。

**ソースファイル（`.s`）とパターンファイル（`.axx`）の両方に適用される。**
パターンファイル側の詳細は「パターンファイルでのマクロ」の節を参照。

axx.py と caxx.c の両方に同一仕様で実装されている。両者の唯一の差は数値の
表現で、Python 版は多倍長整数、C 版は int64。マクロ時演算が 64bit を超える
場合のみ結果が食い違う（マクロ層が出力するのは常にソーステキストなので、
アセンブラ本体の 256bit 式評価には影響しない）。

## 文

すべて行頭（先頭の空白を除く）の `!` から始まる。

| 構文 | 意味 |
|---|---|
| `!def name(p1, p2, p3 = 既定値) { ... }` | マクロ/コンパイル時関数の定義 |
| `!return expr` | 返り値。本体からの早期脱出も兼ねる |
| `!if expr !then { ... } !elif expr !then { ... } !else { ... }` | 条件分岐 |
| `!while expr { ... }` | ループ |
| `!break` / `!continue` | ループ制御 |
| `!set name = expr` | 既存スコープを内側から探して代入、なければ現スコープに作成 |
| `!local name [= expr]` | 現スコープに宣言 |
| `!undef name` | 変数/マクロの削除 |
| `!name(a, b)` | マクロを文として展開 |
| `!include "file"` | マクロ時テキスト取り込み |
| `!error expr` | 展開を中止しエラー |
| `!warning expr` / `!echo expr` | stderr へ出力 |

`{` は必ずヘッダ行の末尾、閉じ `}` は行頭。`; コメント` は文の後ろに書ける。

## 式の中への埋め込み

| 記法 | 意味 |
|---|---|
| `!{expr}` | 値をテキストに展開 |
| `!{expr:04x}` | Python の書式指定を適用 |
| `\!{` | リテラルの `!{` |

## 値と演算子

値は整数と文字列のみ。演算子は C 準拠で
`?:` `\|\|` `&&` `\|` `^` `&` `==` `!=` `<` `<=` `>` `>=` `<<` `>>` `+` `-` `*` `/` `%`
と単項 `-` `+` `~` `!`。`/` と `%` は C と同じ 0 方向切り捨て（`-7/2 == -3`）。
`+` はどちらかが文字列なら連結、`"ab" * 3` は繰り返し。
整数リテラルは `10` / `0x1f` / `0b1010` / `0o17`、`_` 区切り可。
`'A'` は 1 文字なら文字コード、複数文字なら文字列。

## 組み込み関数

`len(s)` `hex(v[,桁])` `str(v)` `int(s[,基数])` `upper(s)` `lower(s)`
`substr(s,開始[,長さ])` `abs(v)` `min(...)` `max(...)` `uid()` `defined(name)`

`substr()` の開始位置と長さは範囲外を切り詰める。負の開始位置は先頭 (0) 扱いで、
Python のような末尾からの逆参照ではない。負の長さは 0 とみなす。

## マクロ内の暗黙変数

| 名前 | 内容 |
|---|---|
| `__id__` | 呼び出しごとに一意な整数（ローカルラベル生成用） |
| `__name__` | マクロ名 |

## CLI

| オプション | 意味 |
|---|---|
| `--no-macro` | マクロ層を完全に無効化 |
| `-P [FILE]`, `--macro-expand [FILE]` | ソースの展開結果だけを出力して終了（省略時は標準出力） |
| `-p [FILE]`, `--macro-expand-pattern [FILE]` | パターンファイルの展開結果だけを出力して終了（省略時は標準出力） |

caxx.c 側も同じオプション名。C 版の `-P` はファイル名を省略できるようにする都合上、
パターンファイルとソースファイルの両方を指定した後ろに置いたときだけ次の引数を
出力先として解釈する（例: `caxx pat.axx src.s -P out.s`）。`-p` も同じ規則。`--no-macro` はソース側・パターン側の両方を無効化する。

## パターンファイルでのマクロ

パターンファイル（`.axx`）もソースと同じマクロ層を通る。命令表は「同じ形の行が
レジスタ番号やオペコードだけ変わって並ぶ」構造になりがちなので、それを手で並べる
代わりに生成できる。

```
!def alu(name, base) {          /* パターンファイルなのでコメントは /* 形式
!local r = 0
!while r < 8 {
!{name} A,R!{r} :: 0x!{base + r:02x}
!set r = r + 1
}
}
!alu("ADD", 0x80)
!alu("SUB", 0x90)
```

ソース側との違いは次の3点だけで、それ以外の構文・組み込み関数・暴走ガードは
まったく同じ。

1. **名前空間が別**。パターン側とソース側は独立したマクロ環境を持ち、互いの
   マクロ・変数は見えない。パターンファイルのマクロがソースの展開結果を変えて
   しまうことがなく、逆に relaxation の各パスで行われるソース側のリセットが
   パターン側のマクロを消してしまうこともない。
2. **文の行のコメントは `/*`**。パターンファイルの流儀に合わせてある。`;` は
   コメントにしない（エラーパターン欄の `v>0xff;2` の `;` が意味を持つため）。
3. **起動条件が厳しい**。パターンファイルでは `!` はパターン変数の記号
   （`ADD A,!d`）なのでほぼ全行に現れる。そのため「`!` があれば起動」ではなく、
   実際にマクロ文として解釈される行・未エスケープの `!{...}`・行頭 `}` の
   いずれかがある場合にだけ起動する。マクロを使っていないパターンファイルは
   マクロ層を素通りし、出力・処理時間とも従来と完全に一致する。

パターンファイル側の `.INCLUDE` はマクロ展開の**後**に処理されるので、マクロで
`.INCLUDE` 行そのものを生成できる。`.INCLUDE` で取り込んだパターンファイルも
それぞれマクロ層を通り、マクロ定義はトップレベルのパターンファイルから継承される。

## 後方互換の扱い

- `!` 行を横取りするのは、キーワード・定義済みマクロ名・直後が `(` の場合のみ。
  VLIW の `!!` および `!F` / `!D` / `!Q` は一切触らない。
- 行頭 `}` を閉じ括弧として扱うのはブロックが開いているときだけ。
- マクロを含まないソースの出力は従来と完全に一致する。

## 制限

- ソース側の `.INCLUDE` はマクロ層を通らないので、マクロ定義ファイルの
  取り込みには `!include` を使う（パターンファイル側の `.INCLUDE` は取り込んだ
  先もマクロ層を通る）。
- `!{a ? b : c:04x}`（三項演算子と書式指定の併用）は現状パースできない。
- 対話モード（ソースファイル省略時）はマクロ層を通らない。

## 暴走ガード

再帰 200 段、`!while` 1,000,000 回、生成行 2,000,000 行、`!include` 深さ 64。
いずれも超過でエラー。エラー発生後は以降のパスで展開を打ち切る。

## 例

```asm
!include "lib.inc"

!def table(name, from, to) {
!{name}:
    !set v = from
    !while v <= to {
        DB !{v}
        !set v = v + 1
    }
}

!def loopblk(n) {
    !if n == 0 !then {
        !return
    }
L!{__id__}_top:
        LD A,!{n}
        LD HL,L!{__id__}_top
}

!def sq(x) {
    !return x * x
}

start:
        LD HL,end
!table("mytab", 1, 5)
!loopblk(2)
        LD HL,!{sq(16)}
end:
        NOP
```
