# axx — 汎用アセンブラ

axx (**A**rbitrary e**X**tended **X** assembler) は*汎用*アセンブラです。アセンブラ自体は
いかなる命令セットの知識も持ちません。プロセッサ固有の知識はすべて、外部の宣言的な
**パターンファイル**に置かれます。あるプロセッサ用のパターンファイルを書けば、そのプロセッサ
用のアセンブラが手に入ります。

## クイックスタート

```sh
git clone https://github.com/fygar256/axx.git
cd axx
make                            # caxx, paxx, axx と man ページをビルド・インストール（sudo を使用）
```

C 実装だけをビルドする場合:

```sh
gcc caxx.c -o caxx -lm -O2      # -lm は必須: 式評価器が libm を使う
```

アセンブル:

```sh
axx z80.axx z80.s -v            # リスティングを標準出力へ
axx z80.axx z80.s -b out.bin    # 生バイナリ
axx x86_64.axx hello.s -o out.o # ELF 再配置可能オブジェクト
```

## 2 つの実装

| 言語 | ファイル | 愛称 | 役割 |
|---|---|---|---|
| Python | `axx.py` | Paxx | リファレンス実装。新機能はまずここに入る |
| C | `caxx.c` | Caxx | はるかに高速。Paxx より遅れることがある |

この 2 つは同じ入力に対して**バイト単位で同一の出力**を生成することを意図しています。
同梱のパターンファイル、テストソース、`test1` スクリプトはまさにそれを検証するために
存在します。`test1` は同梱の 14 組のパターン/ソースの対を両方の実装でアセンブルし、
結果を `cmp` します。

**目次**

1. [適用範囲: axx にできること・できないこと](#1-適用範囲-axx-にできることできないこと)
2. [コマンドライン](#2-コマンドライン)
3. [パターンファイルリファレンス](#3-パターンファイルリファレンス)
4. [VLIW と EPIC プロセッサ](#4-vliw-と-epic-プロセッサ)
5. [アセンブリソースリファレンス](#5-アセンブリソースリファレンス)
6. [式と演算子](#6-式と演算子)
7. [マクロ層](#7-マクロ層)
8. [オブジェクト出力、エクスポートとインポート](#8-オブジェクト出力エクスポートとインポート)
9. [エラー](#9-エラー)
10. [設計ノートと背景](#10-設計ノートと背景)

付録: [A. 例](#付録-a-例) ·
[B. 同梱パターンファイル](#付録-b-同梱パターンファイル) ·
[C. 関連資料](#付録-c-関連資料) ·
[D. ロードマップ](#付録-d-ロードマップ) ·
[E. プロジェクトノート](#付録-e-プロジェクトノート)

---

## 1. 適用範囲: axx にできること・できないこと

この節を最初に読んでください。以降の文書は、境界がどこにあるかを知っていることを前提と
しています。

### 1.1 モデル

パターンファイル内のすべてのパターンは、次の形をしています:

```
instruction :: error_patterns :: binary_list
```

- `instruction` — アセンブリ行と照合する構文。必須。
- `error_patterns` — その行をエラーとする条件。省略可。
- `binary_list` — 出力するバイト列。必須。

例 (x86_64):

```
RET :: 0xc3
```

これがモデルのすべてです。axx の主張は 2 本の独立した脚に支えられており、それぞれ
射程が異なります。

### 1.2 第 1 の脚: 命令構文 — 非常に広い

`instruction` は次の任意の組み合わせです:

- 文字列リテラル（大文字、数字、記号）、
- 整数値で置換可能なシンボル、
- 整数式、
- 整数因子、
- 浮動小数点式。

これは実質的にあらゆる手続き型アセンブリ言語の*表層構文*を表現するのに十分であり、
従来のニーモニック＋オペランド形式に限定されません。`r1 = r2 + r3` は正当な命令
パターンであり、これにより axx はアセンブラとしてだけでなく汎用バイナリジェネレータ
としても使えます。

### 1.3 第 2 の脚: バイナリ生成 — 意図的に狭い

`binary_list` が持つ制御構造は 5 つだけです。代入、三項演算子、`;` 修飾子、
アラインメント、`@@[]` です。これは**万能エンコーダではありません**。実用上の条件は
次のとおりです:

> axx は、命令が機械語に**一対一**で対応するプロセッサをアセンブルできる。

ここから 2 つの帰結が導かれますが、どちらも見落としではなく設計上の決定です:

**パターン記法自体がチューリング不完全です。** これによりパターン*マッチング*の停止性
が保証されます。無制限の計算を必要とするエンコーディングは `binary_list` 単独では
書けません。

**無制限の計算は利用可能ですが、明示的に要求した場合に限られます。** `binary_list` の
要素は `.call name(...)` にでき、これは [3.15 節](#315-ミニ言語-func--call)の
**ミニ言語**で書かれた関数を実行します。ループ、再帰、条件分岐、配列を備えており、
したがってチューリング完全です。マッチングは依然として停止し、エンコーディング段階
だけが固定上限なしに計算できますが、それも 3.15 節のステップ数・深さ・出力の上限に
よって実用上は制限されます。（7 節のマクロ層はこれとは別の、より早い段階であり、
同様に制限されていません。）

**現実のアーキテクチャの中にはモデルの外にあるものもあります。** 構文が書き下せない
からではなく、そのエンコーディングを `binary_list` で生成できないからです:

| プロセッサ | 適用範囲外である理由 |
|---|---|
| Mill CPU | ベルトアーキテクチャ: オペランド参照が実行履歴に依存する |
| ZISC | そもそも命令が存在しない |
| Thinking Machines | 超並列。命令ごとのエンコーディングという対象がない |

量子コンピュータと LISP マシンも適用範囲外です。それらが実行するのは手続き型
アセンブリ言語ではありません。

EPIC および VLIW マシンは機械語にメタレベルの構造を持ち、基本モデルではカバーできま
せんが、後の拡張で扱われます — [4 節](#4-vliw-と-epic-プロセッサ)を参照。

### 1.4 他のツールとの位置関係

axx は LLVM、CGen、customasm よりも**低い抽象度**で動作します。すぐに広く使えると
いう意味での「汎用アセンブラ (general-purpose assembler)」ではなく、すべてのター
ゲットの下に 1 つの共通機構を持つという意味での「汎用アセンブラ (general assembler)」
です。

特定チップ向けの手書きアセンブラが行う最適化は行いませんし、構造化アセンブリや関数型
アセンブリの構成要素を手続き型に落とすこともしません。ただし完全なマクロ層は備えて
います（7 節）。

パターンファイルとソースファイルが分離しているため、パターンファイルを差し替えるだけ
で同じソースを別のプロセッサ向けにアセンブルでき、共通のソース言語で複数のプロセッサ
をターゲットにできます。それが*有用*かどうかは、パターンファイルにどれだけ労力を
かけるかに完全に依存します。

### 1.5 パターンファイル記述に関する実践的な注意

大きな ISA 用のパターンファイルを書くのは長い作業ですが、完成すれば恒久的に使え、
再利用できます。フォーマットは宣言的かつ機械的なので、AI による生成の対象として
妥当です。小さな ISA であれば、モデルにパターンファイルを生成させて短時間で動作
するアセンブラを得られます。

あるパターンの表現が難しいと分かった場合、次の回避策が常に使えます: 本当に評価が
必要なオペランドだけを渡し、残りは文字列リテラルとして書くことです。構造化に馴染ま
ない ISA の一部は、単に列挙してしまえばよいのです。

実行プラットフォームは重要ではありません。DOS 行末の `chr(13)` は無視されますし、
Paxx は Python 3 が動く場所ならどこでも動きます。

---

## 2. コマンドライン

### 2.1 書式

```
axx [-h] [--osabi ELF_OSABI] [-b OUTFILE] [-e EXPORT_TSV]
    [-E EXPORT_ELF_TSV] [-i IMPORT_TSV] [-o OBJ_FILE] [-f {32,64}]
    [-m MACHINE] [-v] [-d] [-g] [--no-macro] [-P [FILE]] [-p [FILE]]
    patternfile [sourcefile]
```

| 引数 | 意味 |
|---|---|
| `patternfile` | パターン定義ファイル (`.axx`)。必須。 |
| `sourcefile` | アセンブリソース (`.s`)。省略すると標準入力から読む（プロンプトモード）。 |

| オプション | 意味 |
|---|---|
| `-b OUTFILE` | 生バイナリを書き出す |
| `-o OBJ_FILE` | ELF 再配置可能オブジェクトを書き出す。クラスは `-f` で選択 |
| `-f {32,64}` | `-o` の ELF クラス。既定値 64 |
| `-m MACHINE` | ELF の `e_machine` 値。既定値 62 (`EM_X86_64`) |
| `--osabi ELF_OSABI` | ELF OSABI。既定値 FreeBSD。FreeBSD/Linux、大文字小文字を区別しない |
| `-e EXPORT_TSV` | ラベルを TSV にエクスポート（プレーン形式） |
| `-E EXPORT_ELF_TSV` | ラベルを TSV にエクスポート（ELF セクションフラグ付き） |
| `-i IMPORT_TSV` | TSV からラベルをインポート |
| `-v`, `--verbose` | アセンブルリスティングを標準出力へ出力（既定: 無出力） |
| `-d`, `--debug` | デバッグ出力: 前方参照フォールバック、リラクゼーションログ |
| `-g`, `--gen-debug` | DWARF (`.debug_info`/`.debug_abbrev`/`.debug_line`) を出力。`-o` と ELF64 が必要 |
| `--no-macro` | ソース側・パターン側の両方でマクロ層を無効にする |
| `-P [FILE]` | ソースをマクロ展開し、アセンブルせずに書き出す |
| `-p [FILE]` | パターンファイルをマクロ展開し、アセンブルせずに書き出す |
| `-h`, `--help` | 使い方 |

出力オプションを何も指定しない場合、何も書き出されません。実行結果を目に見えるよう
にするのが `-v` です。

### 2.2 ELF 出力

`-o` は再配置可能オブジェクトを生成し、FreeBSD と Linux で動作します。x86_64 に
限定されません。`-m` は axx が再配置番号を持つ任意のアーキテクチャを受け付けます:

| 値 | マシン | 値 | マシン |
|---|---|---|---|
| 3 | i386 | 43 | SPARCV9 |
| 4 | M68K | 62 | x86-64（既定） |
| 20 | PowerPC | 183 | AArch64 |
| 21 | PowerPC64 | 243 | RISC-V |
| 22 | s390x | | |
| 40 | ARM | | |
| 42 | SuperH | | |

`-f` は `-m` とは独立に ELF32 か ELF64 を選びます。選んだマシンにとって慣例的でない
組み合わせ（たとえば実際の x32 ABI レイアウトである `-m 62 -f 32`）も、警告付きで
受け入れられます。

### 2.3 C 版との差異

`caxx` は `axx.py` と同じオプション名を取りますが、次の点が異なります:

- `-h` / `--help` は受け付けません。使い方は引数なしで `caxx` を実行してください。
- `-d` / `--debug` は未実装です。
- `-P` の後のファイル名は省略可能なため、`caxx` はパターンファイルとソースファイルの
  両方が既に与えられている場合にのみ、次の引数を出力ファイルとして扱います:
  `caxx pat.axx src.s -P out.s`。`-p` も同じ規則に従います。

### 2.4 プロンプトモード

ソースファイルを指定しない場合、axx は `>>` プロンプトで端末からアセンブリ行を読み
ます。`?` はラベルテーブルを表示します。このモードではマクロ層はバイパスされます。

---

## 3. パターンファイルリファレンス

### 3.1 構造

パターンファイルはプロセッサ記述ファイルです。小さな ISA 記述言語 (ISADL) — アセンブリ
テキストと機械語の対応関係を記述するメタ言語です。

```
instruction :: error_patterns :: binary_list
instruction :: error_patterns :: binary_list
:
```

3 つのフィールドは `::` で区切られます。`error_patterns` は空でも構いませんが、完全に
省略すると行には `::` が 1 つだけになり、それが 2 フィールド形式です。

**コメント。** `/*` がコメントを開始します。*同じ*行の後方に `*/` が現れた場合、
`/*` と `*/` の間のテキストだけが除去され、行の残りは通常どおり読まれます —
`/* note */ MORE` は `MORE` を残します。そうでなければコメントは本物のブロックコメント
のように振る舞い、`*/` を含む行が見つかるまで後続の行へ続きます。終了行の `*/` より
後のテキストは再び通常どおり読まれます。

1 つの本物のブロックを開く代わりに、コメント行すべての先頭に `/*` を書く古いパターン
ファイルとの後方互換性のため、次のいずれかが成り立つときコメントは自分の行を超えて
**延長しません**:

- すぐ次の行自体が `/*` で始まる、または
- ファイル内のそれ以降どこにも `*/` が現れない。

どちらの場合も行は自分の `/*` の位置で切り詰められ、何も持ち越されません。これは元々の
単一行の振る舞いと一致します。そしてそのような行の連なり全体が同じように扱われます。
連なりの最終行の直後が通常の（コメントでない）内容であっても同様です。

要するに、すべてのブロックコメントを自分の `*/` で閉じることも、すべてのコメント行を
自分の `/*` で開くことも、どちらも完全にサポートされた記法であり、1 つのファイル内で
自由に混在させられます。

### 3.2 パターンの順序は問わない

ディレクティブの定義は順序に依存**します** — 後の `.setsym` は前のものを上書きし、
`.check` は現れた位置から適用されます。**パターンは順序に依存しません。**

axx は最初にマッチしたパターンで止まりません。すべてのパターンを試し、マッチに成功した
ものそれぞれにスコアを付け、最良のものを出力します。スコアは次のタプルです:

```
(n_expr, -n_lit, n_sym)
```

ここで `n_expr` は `!` 前置の式キャプチャの数、`n_lit` はマッチしたリテラル文字の数、
`n_sym` はシンボルキャプチャの数です。**最小の**タプルが勝ち、これは次のように読めます:

1. 式キャプチャが最も少ないもの。
2. 同点なら、リテラル文字のマッチが最も多いもの。
3. 同点なら、シンボルキャプチャが最も少ないもの。

言い換えれば、どの順序に置かれていようと最も具体的なパターンが勝ちます。次が与えられ
たとき

```
MOV A,!d :: 0xAA,d
MOV A,B  :: 0xBB
```

`mov a,b` は `0xBB` を、`mov a,5` は `0xAA 0x05` を出力し、2 行を入れ替えても何も
変わりません。

これにより、テーブル駆動のアセンブラが通常要求する管理作業がなくなります — 特殊形が
一般形より前に来るように命令テーブルを手で並べ替える必要はありません。これが最も
効いてくるのは大きなパターンファイルで、特殊ケースがそれを上書きする一般規則から
遠く離れている場合です。

### 3.3 大文字小文字と変数

`instruction` フィールドでは:

| 書き方 | 意味 |
|---|---|
| 大文字、数字、記号、エスケープされた文字 | 文字定数。大文字はソース中の大文字・小文字両方にマッチする。 |
| 小文字の名前 | その位置の**シンボル**の値 |
| `!x` | その位置の**整数式**の値 |
| `!!x` | その位置の**整数因子**の値 |
| `!Fx` | **32 ビット**浮動小数点式の IEEE-754 ビットパターン |
| `!Dx` | **64 ビット**浮動小数点式の IEEE-754 ビットパターン |
| `!Qx` | **128 ビット**浮動小数点式の IEEE-754 ビットパターン |
| `!Ex` | `.enum` で宣言された**列挙オペランドリスト**の値（3.7.1 節） |
| `!S{{name}}x` | `.sub` で宣言された**サブテーブル** `name` のマッチしたエントリの値（3.7.2 節） |

キャプチャされた値は `error_patterns` と `binary_list` から名前そのもので参照します —
そこで `!` 前置を繰り返すことはありません。すべての変数はパターン行ごとに 0 にリセット
されるので、マッチしなかった省略可能オペランドは 0 として読まれます。

**変数名。** 上では変数を `x` と書いていますが、名前はより長くできます。小文字で始まり、
小文字・数字・`_` が続くので、`abcdef`、`var1`、`var_2` はすべて変数名です。同じ名前が
`.check`、`.enum`、`.clrcheck`、`.clrenum`、`.map`、`.reloc`、`.clrreloc`、
`.free` でも使われます。

```
.check::reg::BC,DE,HL,SP
INC reg,!imm_1 :: :: 0x03|reg,imm_1
```

長さは関係ありません。`a` も `var_2` も同じ規則で読まれます。`error_patterns` と
`binary_list` では、小文字で始まる連続したテキストは変数です。その行がキャプチャした
かどうかは問いません — キャプチャされていない名前は 0 として読まれ、マッチしなかった
省略可能オペランドと同じです。事前に何も宣言する必要はありません。

したがって、そこに現れる小文字だけの単語は決してラベルではありません。連なりは小文字・
数字・`_` でない最初の文字で終わり、続く文字がラベルに含まれうるもの（大文字または `.`）
であれば、その単語全体はやはりラベルになります — よって `aB` と `a.b` はラベルとして
読まれ、`a1` と `var_2` は変数です。パターン行が名指しする必要のあるラベルは、どこかに
大文字か `.` を含んでいなければなりません。

アセンブリ行は、ラベルとセクション名を除いて大文字小文字を区別しません。

エスケープ文字 `\` は `instruction` の内部で使えます。

### 3.4 error_patterns

エラーを発生させる条件をカンマ区切りで並べ、それぞれ `;` の後にエラーコードを付けます:

```
a>3;4,b>7;5
```

ここで `a>3` はコード 4 を、`b>7` はコード 5 を発生させます。`!=` を含む比較演算子が
使えるので、`a!=3;2` も `(s&0xf!=0)||(s>>4)>3;9` もどちらも有効です。

コード 1、2、3、5、6 にはメッセージ本文があります。[9 節](#9-エラー)を参照。それ以外の
コード — 4 や 7 以上を含む — もエラーを発生させアセンブルを中断しますが、空のメッセージ
を表示します。本文のある 5 つのどれかを選ぶか、`axx.py` と `caxx.c` の `ERRORS` テーブル
に自分のものを追加してください。

`error_patterns` は浮動小数点モードで評価されるので、値は IEEE-754 倍精度ビットパターン
として運ばれます。ビット演算子とシフト演算子は内部で補正するので、`(8>>2)>0;2` は書いた
とおりに評価されます。

### 3.5 binary_list

カンマ区切りの出力値です。`0x03,d` は `0x03` に続けて `d` を出力します。

```
ADD A,R!n :: n>7;5 :: n|0x68
```

`add a,r1` は `0x69` を出力します。`n>7` はコード 5（レジスタが範囲外）を発生させます。

- **空要素**はアラインメントを行います。先頭のカンマ、あるいは `0x12,,0x13` は、
  正確なアドレスまでパディングします。
- `;` で始まる要素は、**その値が 0 のとき抑制されます**。
- `;;` で始まる要素は、**評価されますが決して出力されません**。`0x11,;;n,0x22` は
  2 ワードを出力し、`;;n` だけの `binary_list` は何も出力しません。

#### 3.5.1 `@@[]` — 繰り返し

`@@[n,<str>]` は `<str>` を n 回繰り返します。`%%` は繰り返しインデックスで、`%0` は
それを 0 にリセットします。

#### 3.5.2 テキストテンプレート — 文字列の出力

`binary_list` の要素は、式の代わりにダブルクォートで囲んだ**文字列**にできます。これは
**テキスト**を構築します。そのソース行のアセンブル結果を、別のニーモニック構文で書いた
ものです。これによりパターンファイルはソース間トランスレータになります。同じテキストは
**バイナリ**としても、1 バイトにつき 1 ワードで出力されるので、文字列自体がその行の
出力になります。

```
MOV R!r,!e:: "LD R{{r}},0x{{.hex(e)}}"
```

`MOV R1,0x10` が与えられると、このパターンは `LD R1,0x10` をテキストとして、また
10 バイト `4c 44 20 52 31 2c 30 78 31 30` として出力します。

文字列は他の要素と同じくリストの 1 要素なので、式と自由に混在させたり、`@@[...]` で
繰り返したり、複数回書いたりできます。文字列の内側のカンマは要素を区切りません。

```
MOV A,!e::"load a,{{e}}\n",0x12,0x13
```

```
MOV A,3   ->  6c 6f 61 64 20 61 2c 33 0a 12 13   ("load a,3\n" の後に 0x12 0x13)
```

**置換されるのは `{{ }}` だけです。** 文字列内のそれ以外はすべて書いたとおりのテキスト
です — 大文字、小文字、波括弧の外の `.hex(e)`、そのすべてがそのまま出てきます。他に
読まれるのはバックスラッシュエスケープだけです。

| 形式 | 意味 |
|---|---|
| `{{<expr>}}` | 式を評価して 10 進で挿入 |
| `{{.hex(<expr>)}}` | 値の 16 進数字 |
| `{{.dec(<expr>)}}` | 値の 10 進数字 |
| `{{.bin(<expr>)}}` | 値の 2 進数字 |
| `{{.float(<expr>)}}` | 値を 10 進 128 ビット浮動小数点数、有効数字 34 桁として（`16` は `16.0` になる） |
| `{{<name>}}` `{{<name>[<expr>]}}` | 文字列シンボルまたは配列シンボル、さもなければパターン変数 — 下記参照 |
| `{{.index <name>[<expr>]}}` | `{{<name>[<expr>]}}` が使う添字そのものを 10 進で（3.6.1 節） |
| `\n` `\t` `\r` `\\` `\"` | 改行、タブ、復帰、バックスラッシュ、ダブルクォート |
| `\<char>` | その他の `<char>` をそのまま |

4 つの変換は数字だけを出力し、基数プレフィックスは付かないので、プレフィックスは自分で
書きます — `0x{{.hex(e)}}`、`0b{{.bin(e)}}`。

`.float` は値を 10 進 128 ビット浮動小数点数として描画します。有効数字 34 桁、偶数丸め
です。小数部のない値にも小数部が付くので、`16` は `16.0` と書かれます。34 桁を超える
場合、あるいは非常に小さな値では指数形式に切り替わります
(`1.234567890123456789012345678901235e+36`)。両実装はバイト単位で同一のテキストを
生成します。

**`{{ }}` 内の名前。** 波括弧の中に単独で書かれた名前は、次の順序で解決されます:

1. **文字列シンボル** — `.setsym::<name>::"<text>"` — そのテキストを挿入する;
2. **配列シンボル** — `[<expr>]` 付きで、そのインデックスの項目;
3. それ以外 — **式**。したがってパターン変数はその値を 10 進で与える。

`R{{r}}` が `R1` になるのはこのためです。`R` は波括弧の外なのでリテラルであり、`r` は
`!r` で束縛されています。

数値の `.setsym` シンボルは意図的に規則 1 では参照されません。そのため、ただの単語が
黙って数値に置き換えられることはありません。数値シンボルの値が欲しいときは `{{#NAME}}`
と書きます。

**文字列シンボル。** `.setsym` は値フィールドがダブルクォートで始まるとき、数値ではなく
テキストを格納します:

```
.setsym::x::"LD"
MOV R!r,!e::"{{x}} R{{r}},{{.dec(e)}}"        ->  LD R0,12
```

```
.setsym::x::"LO"
.setsym::y::"AD"
MOV R!r,!e::"{{x}}{{y}} R{{r}},{{.dec(e)}}"   ->  LOAD R0,12
```

`mov r0,12` が与えられると、これらは `LD R0,12` と `LOAD R0,12` を生成します。文字列
シンボルは数値を持たないので式の中には現れられません。テキストテンプレートのために存在
します。`.clearsym` は数値シンボルと同じように文字列シンボルを削除します。

`MOV R1,0x10` が与えられたとき:

```
MOV R!r,!e:: "LD R{{r}},0x{{.hex(e)}}"    ->  LD R1,0x10
MOV R!r,!e:: "LD R{{r}},{{.dec(e)}}"      ->  LD R1,16
MOV R!r,!e:: "LD R{{r}},0b{{.bin(e)}}"    ->  LD R1,0b10000
MOV R!r,!e:: "LD R{{r}},0f{{.float(e)}}"  ->  LD R1,0f16.0
```

テキストの行き先:

- 行末の改行は書いたぶんだけ入ります。1 行が 1 行になるようにしたいときは、テンプレート
  ごとに `\n` を書く代わりに `.eol`（3.17 節）を使えます。
- `.ascii`（4.4 節）とまったく同じようにバイトとして出力されます。描画されたテキストは
  UTF-8 でエンコードされ、各バイトが 1 出力ワードになるので、その行はロケーションカウンタ
  を進め、バイナリと ELF 出力に入ります。出力ワード幅 (`.bits`) より広いバイトは警告付き
  で切り詰められます。`\0` や `\xHH` のエスケープはありません — テキストでないバイトは、
  文字列の隣にリスト内の数値として書きます。
- `-v` なしのとき、描画された行だけが標準出力にも書かれるので、翻訳されたプログラムを
  単にファイルへリダイレクトできます。
- `-v` ありのとき、その行の診断出力の末尾、`//` の後にダブルクォートで囲まれて現れます:
  `... MOV R1,0x10 // "LD R1,0x10"`。そこではエスケープされて表示されます — 改行は
  改行そのものではなく `\n` と読めます — ので診断出力は 1 行に収まり、複数の文字列を
  持つ行はフィールドに書かれたとおりに分けて表示されます: `... // "A","B"`。

  `;;` 要素は評価されますがテキストを含めどこにも出力されません。`;` 付きの文字列は、
  描画結果が空のときスキップされます。

このように 1 つのパターンファイルが 2 つの用途を同時に果たします。翻訳のための標準出力
へのテキストと、アセンブルのためのバイナリ内の同じ文字列です。

```
NOP  :: "ABC"
DW !e:: e,e>>8
```

```
NOP        ->  0x41 0x42 0x43
NOP        ->  0x41 0x42 0x43
L1:
DW L1      ->  0x06 0x00        ; L1 は 6。各 NOP の 3 バイトが数えられている
```

### 3.6 シンボル

```
.setsym :: name :: value
```

1 つのディレクティブがあらゆる種類のシンボルを定義します。どの種類になるかは値フィールド
の見た目で決まります:

| 値フィールド | 定義されるもの |
|---|---|
| `0x20`, `#OTHER+1` | **数値シンボル** — 任意のパターン式 |
| `"LD"` | **文字列シンボル** — テンプレート用のテキスト (3.5.2) |
| `[1,"A",#B]` | **配列シンボル** — 数値とテキストのリスト (3.6.1) |
| `other` | その文字列シンボルまたは配列シンボルの**コピー**、どちらでもなければ**その名前を保持する文字列シンボル** (3.6.1) |
| `r0,r1,r2` | **集合** — 名前のリスト (3.6.2) |
| `a&b`, `a\|b`, `a^b`, `a+b`, `a-b` | 他の集合から計算された**集合** (3.6.2) |

特殊形 — クォート、ブラケット、裸の名前、名前のリスト、集合式 — は当てはまる場所では
そのように認識されます。どれにも当てはまらないフィールドは数値式として読まれます。
各形式は隣に記した節で説明されています。

シンボル名には文字、数字、記号文字を含められます。シンボルは大文字小文字を区別しません。
同じ名前の後の定義は前のものを上書きするので、同じ識別子がファイルの異なる領域で異なる
意味を持てます:

```
.setsym::B::0
.setsym::C::1
ADD A,s              /* ここでの C は 1

.setsym::NZ::0
.setsym::Z::1
.setsym::NC::2
.setsym::C ::3
RET s                /* ここでの C は 3
```

別のシンボルからシンボルを定義するには `#` を使います:

```
.setsym ::symbol1 ::1
.setsym ::symbol2 ::#symbol1
```

ダブルクォートで始まる値フィールドは**文字列シンボル**になります:

```
.setsym::x::"LD"
```

文字列シンボルは数値ではなくテキストを保持するので、式の中には現れられません。テキスト
テンプレート（3.5.2 節）で使われ、そこで `{{x}}` は `LD` を挿入します。`.clearsym` は
他のシンボルと同様にこれらを削除します。

#### 3.6.1 配列シンボル

`[` で始まる値フィールドは**配列シンボル**になります。その項目は数値式、素の名前、
文字列リテラル、またはその混在にできます:

```
.setsym::x::[1,2,3,4,5]
.setsym::y::["A","B","C","D","E"]
.setsym::regs::[R0,R1,R2]          /* ["R0","R1","R2"] と同じ */
```

項目はトップレベルのカンマで区切られます。`"..."` の内側や、入れ子のブラケット・括弧の
内側のカンマは分割しません。**素の名前**（`R0`、`_tmp` など）の項目は、書かれたままの
綴りのテキストとして保持されます。したがって `[r0,r1]` は小文字のままです。それ以外の
項目は通常のパターン式なので、先に定義したシンボルが使えます
(`.setsym::m::[1,#BASE,#BASE+1]`)。名前そのものではなく、その名前に `.setsym` や `.map`
で与えた数値が欲しいときは `#R0` と書きます。

項目は `[<index>]` で選択し、0 から数えます。インデックス自体が式なので、パターン変数で
駆動できます:

| 場所 | 書き方 | 例 |
|---|---|---|
| テキストテンプレート (3.5.2) | `{{x[3]}}` | `{{y[3]}}` → `D` |
| 式 | `#x[3]` | `MOV !e :: #x[3],e` は `4` を出力 |

テンプレートでは、数値項目は 10 進で書かれ、文字列項目はテキストとして挿入されます。
式の中の `#x[3]` は数値項目を必要とします — 文字列項目は数値を持たないので、そこでは
エラーです。テンプレート中でインデックスのない配列名は、全項目を `,` で連結したものに
展開されます。

**添字の解決順。** テンプレートの `{{<name>[<expr>]}}` と `{{.index <name>[<expr>]}}`
では、**文字列リテラル**で書かれた添字がまずその中身に開かれます。したがって
`arrb["CX"]` は `arrb[CX]` と同じに読まれます。テンプレートの中ではパターン自身の文字列の
閉じ `"` が先に来るので、リテラルは `{{.index arrb[\"CX\"]}}` と逃がして書きます。開かれた
テキストは次の順で解決されます:

1. **文字列シンボル**の名前 — その中身を添字として読み直す;
2. **パターン変数**の名前 — 4 へ（変数の値を使う）;
3. **その配列の項目**の名前 — その項目の位置。
   それが無ければ、同じ名前の `.setsym` / `.map` の**数値シンボル** — その値;
4. どれでもない — ふつうの**式**。

名前を添字として使えるのは規則 3 のおかげです。

```
.map::r::AX,BX,CX                  /* AX=0, BX=1, CX=2 と .check::r */
.setsym::arrb::[R0,R1,R2]
.setsym::arra::[AX,BX,CX]
.setsym::var1::BX                  /* var1 は BX という名前（3.6 節） */
```

このとき `{{arrb[var1]}}` は `R1` です。`var1` は BX という名前（規則 1）、`BX` は
`arrb` の項目ではないが `.map` が定義した数値シンボル 1（規則 3）なので、添字は 1 に
なります。`{{arra[var1]}}` は規則 3 の前半で `BX`（`arra` の位置 1 の項目）です。
`{{arrb[r]}}` は規則 2 なので、
`MOV r,!e::"LD {{arrb[r]}},0x{{.hex(e)}}"` は `mov bx,5` を `LD R1,0x5` にします。

**`.index` — 添字そのもの。** `{{.index <name>[<expr>]}}` は、まったく同じ規則で添字を
解決し、項目の代わりにその添字を 10 進で書きます。引き当ての「名前から番号へ」の向きです:

```
.setsym::var1::BX
TEST v::"LD A,{{.index arra[v]}}"     /* TEST CX  ->  LD A,2 */
                                      /* {{.index arrb[var1]}}    ->  1 */
                                      /* {{.index arrb[\"CX\"]}}   ->  2 */
                                      /* {{.index arra[\"CX\"]}}   ->  2 */
```

最後の 2 つは規則 3 の両半分が同じ答えに着くところです。`CX` は `arrb` の項目ではないので
その数値シンボル 2 が添字になり、`arra` では位置 2 の項目そのものです。

`.index(<name>[<expr>])` と括弧で括って書いてもよく、2 つの形は同じです。他の参照と
同じように、配列が存在し、添字が範囲内でなければなりません。

範囲外のインデックスや、配列でない名前への `name[...]` はエラーとして報告されます。
`.clearsym` は他のシンボルと同様に配列シンボルを削除します。

**コピー。** 配列シンボルまたは文字列シンボルの名前だけを書いた値フィールドは、それを
コピーします:

```
.setsym::x::["R1","R2","R3"]
.setsym::y::x                  /* y は x のコピーになった */
```

コピーは独立しているので、後で `x` を再定義しても `y` は元のままです。配列でも文字列
シンボルでもない素の名前は、**その名前を保持する文字列シンボル**になります:

```
.setsym::var1::BX              /* var1 は BX という名前。{{var1}} は BX と出る */
```

これによって名前を持ち回り、添字として使えます（上の「添字の解決順」を参照）。その名前に
`.setsym` や `.map` で与えた数値が欲しいときは `#BX`、`BX` という*ラベル*の値が欲しい
ときは `BX+0` のように（名前ひとつではなく）式にして書きます。

**配列は要素リストの代わりになれます。** ディレクティブが列挙された名前のリストを取る
ところ — `.check`、`.enum`、`.map` — では、配列シンボルの名前を書くとその場でその内容に
展開されます:

```
.setsym::regs::["R0","R1","R2","R3","R4"]
.check::x::regs           /* .check::x::R0,R1,R2,R3,R4 と同じ */
.map::x::regs::1<<x       /* .map::x::R0,R1,R2,R3,R4::1<<x と同じ */
.enum::f::regs::…
```

配列とただの名前は 1 つのリスト内で混在できます (`.check::x::regs,SP`)。数値項目はその
10 進表記を寄与します。したがってレジスタ名のリストを一度だけ書いて、それを必要とする
すべてのディレクティブで使えます。

Z80 レジスタの例:

```
.setsym ::B ::0
.setsym ::C ::1
.setsym ::D ::2
.setsym ::E ::3
.setsym ::H ::4
.setsym ::L ::5
.setsym ::A ::7
.setsym ::BC ::0x00
.setsym ::DE ::0x10
.setsym ::HL ::0x20
.setsym ::SP ::0x30
```

シンボルには句読点や数字を含められます: `.setsym ::$s5:: 21`。

**クリア。** `.clearsym::ax` は `ax` を未定義にします。引数なしの `.clearsym` はすべてを
クリアします。

**文字集合。** `.symbolc::<characters>` はシンボルに使える文字集合を拡張します。既定は
文字、数字、および `_%$-~&|` です。

`-` が既定の集合に入っていることに注意してください。これがシンボルの直後に負の変位を
続けられる理由です — マッチャは最長のシンボル接頭辞を先に試し、失敗すればフォールバック
します:

```
MOV EAX,[RBX-8]      ; x86_64.axx -> 8b 83 f8 ff ff ff
LD A,(IX-5)          ; z80.axx
```

同じ規則により、パターンがシンボルを期待する位置に負の値を書くと — 命令に即値形式が
ないときの `ASR #-1` など — 範囲エラーではなく `undefined symbol: '#-1'` として報告
されます。

#### 3.6.2 集合

カンマで区切られた名前だけのリストである値フィールドは、**集合**になります:

```
.setsym::a::a1,a2,a3
.setsym::b::b1,b2,b3,a2
```

集合はそれらの名前を項目とする配列シンボルなので、3.6.1 節のすべてが当てはまります —
`.check::x::a`、`.map::x::a::…`、`.enum::f::a::…`、テンプレート中の `{{a}}` や
`{{a[0]}}` がすべて動きます。項目は数値ではなく名前なので、式の中の `#a[0]` は他の
テキスト配列と同様に文字列項目として報告されます。名前は他のシンボル名と同様に大文字化
され、リスト中で繰り返された名前は 1 つだけ保持されます。リスト中の名前がそれ自体集合
である場合はその場で展開されるので、`.setsym::c::a,b` は 2 つの和集合になります。

**集合代数。** 集合は 5 つの演算子で結合できます:

| 書き方 | 結果 |
|---|---|
| `a&b` | 積集合 — 両方に含まれる名前 |
| `a\|b` | 和集合 — どちらかに含まれる名前 |
| `a+b` | 和集合、`a\|b` と同じ |
| `a^b` | 対称差 — 一方にだけ含まれる名前 |
| `a-b` | 差集合 — `a` にあって `b` にない名前 |

```
.setsym::x::a&b        /* A2                      */
.setsym::y::a|b        /* A1,A2,A3,B1,B2,B3       */
.setsym::z::a^b        /* A1,A3,B1,B2,B3          */
.setsym::t::a+b        /* A1,A2,A3,B1,B2,B3       */
.setsym::q::a-b        /* A1,A3                   */
```

各結果は独立したコピーなので、後で `a` を再定義しても `x` … `q` は元のままであり、
`.setsym::a::a|b` は安全です。

順序は各名前が最初に現れた順に従います。左オペランドの名前が先で、次に右オペランドの
名前です。演算子は固有の優先順位を持たず左から右へ適用されるので、`a&b|c` は
`(a&b)|c` を意味します。

**フィールドが集合でない場合。** 集合としての解釈は通常の数値解釈より先に試され、
フィールドが集合になりえない場合は必ず数値解釈に譲るので、以前アセンブルできていた
ものの意味が変わることはありません:

- カンマリストの各項目は名前でなければなりません — `.setsym::X::1,2` のように数字で
  始まる項目を含むリストは式評価器に委ねられます;
- 各演算子のオペランドは既存の集合を指す裸の識別子でなければなりません —
  `.setsym::X::#N1+#N2` は従来どおり 2 つの数値シンボルを加算します;
- 単一の名前を保持するフィールドはコピー (3.6.1 節) であり、1 要素の集合ではありません。
  それが欲しいときは `.setsym::a::["A1"]` と書きます。

`-`、`&`、`|` はシンボル名の中に現れうるので、オペランドが集合と認識されるのは文字・
数字・`_` だけで綴られている場合に限られます。

### 3.7 シンボルチェック (`.check`)

```
.check::x::r1,r2,r3
```

`x` がキャプチャする位置に現れてよいものを制限します。それ以外はエラーです。
`.clrcheck::x` は制限を解除します。リストは配列シンボル（3.6.1 節）や集合（3.6.2 節）、
またはそれらとただの名前の混在にできます。

**小文字の変数が 2 つ以上のオペランドクラスで再利用されるときは、必ず `.check` を
設定する価値があります。** これがないと変数はパターンファイルのどこかで定義された
*任意の*シンボルを受け付けてしまい、意味をなさないオペランドの組み合わせが拒否される
代わりに黙って誤ったバイト列にアセンブルされます。`.check` は位置依存で、変更される
まで有効であり続けるので、変数の意味が変わる各地点で新しい `.check`（または
`.clrcheck`）を置いてください。

同じニーモニックを共有する異なる幅のレジスタ:

```
.setsym::AL::0x00
.setsym::BL::0x01
.setsym::AX::0x00
.setsym::BX::0x01
.check::s::AL,BL
.check::t::AX,BX
MOV s,!a :: 0xb0|s,a
MOV t,!a :: 0xb8|t,a,a>>8
```

これにより `mov al,0x12` と `mov ax,0x1234` が区別されます。

#### 3.7.1 列挙オペランドリスト (`.enum`)

`.check` が位置を集合の中の*1 つ*のシンボルに制限するのに対し、`.enum` はそれらの
*リスト*を取る位置を宣言します — 68000 の `MOVEM` のようなレジスタリストです。

```
.enum::<変数>::<要素,要素,...>::<式>
```

要素リストは、名前を書き並べる代わりに配列シンボル（3.6.1 節）でも構いません。

要素の順序が列挙順序であり、それがソース中の `-` 範囲の意味になります。`<式>` の中で
各要素名は次のように評価されます

- その要素がソースのリストに現れていれば、その `.setsym` 値、
- 現れていなければ `0`。

リストは `!E<変数>` でキャプチャされます。`<式>` の値が変数に束縛され、その後は他の
すべてのキャプチャとまったく同様に、`error_patterns` と `binary_list` から裸の文字で
参照されます。

```
.setsym::A0::0x01
.setsym::A1::0x02
.setsym::A2::0x04
.setsym::B0::0x08
.setsym::B1::0x10
.setsym::B2::0x20
.enum::x::A0,A1,A2,B0,B1,B2::A0|A1|A2|B0|B1|B2
MOVEM !Ex :: :: @@[8,*(x,%%)]
```

```
movem a0-a2,b2       -> 27 00 00 00 00 00 00 00   (0x01|0x02|0x04|0x20)
movem a0-a2/b2       -> 同じ。`,` と `/` はどちらも区切り文字
movem a0/a2          -> 05 00 00 00 00 00 00 00
movem a1             -> 02 00 00 00 00 00 00 00
```

式は通常の axx 式なので、要素はビットマスクとして組み合わさる必要はありません。手で
重み付けすれば、代わりに位置表現のエンコーディングになります:

```
.enum::y::A0,A1,A2,B0,B1,B2::A0*100000+A1*10000+A2*1000+B0*100+B1*10+B2
```

注意点。

- **区切り文字。** `,` と `/` はどちらも要素を区切り、`first-last` は列挙順序上の範囲
  です。区切り文字は別の要素が後続する場合にのみ消費されるので、パターンはリストの後で
  `,` を使い続けられます: `PUSH !Ex,-(SP)` は `push a0-a2,-(sp)` にマッチします。
  要素が後続しない `-` も同様にパターンの残りに委ねられるので、`SUBT !Ev-!d` は
  `subt a0-9` にマッチします。
- **逆順の範囲** (`a2-a0`) は範囲として読まれません。`-` はパターンに委ねられます。
- **ソースに現れるが `.setsym` 定義を持たない要素**は、黙って 0 を寄与するのではなく、
  パターンをマッチさせなくします。
- `<式>` の中の `#name` は素の `.setsym` 値であり、その要素が現れたかどうかに影響され
  ません。
- `.enum` は `.check` と同じく位置依存です。同じ変数に対する後の `.enum` は前のものを
  置き換え、`.clrenum::x`（または引数なしの `.clrenum`）はそれを削除します。

#### 3.7.2 サブテーブル (`.sub` / `.return`)

サブテーブルは、`instruction` フィールドの一位置に差し込める、名前の付いた選択肢の
集合です。`.check` が位置を 1 つの*シンボル*に制限するのに対し、サブテーブルはその位置を
まるごと 1 つの*パターン*にでき、各選択肢が自分の値を持ちます。

```
.sub::<name>
<pattern>::<value list>
...
.return
```

テーブルは `!S{{<name>}}<変数>` で参照されます。すべてのエントリが書かれた順に試され、
エントリの `<pattern>` が参照の位置でマッチされ、その `<value list>` の値が変数に束縛
されます。その後は他のすべてのキャプチャとまったく同様に、`error_patterns` と
`binary_list` から裸の文字で参照されます。

```
MOV!S{{sub1}}x A,!e :: 0x01,x<<8,x,e<<8,e
.sub::sub1
R1::0x01,0x01
R2::0x02,0x02
R3::0x03,0x03
.return
```

```
movr1 a,0x22         -> 01 00 01 00 22
movr2 a,0x33         -> 01 00 02 00 33
```

2 要素以上の値リストは 1 つの値にパックされます。最初の要素が最上位で、それぞれ
`.bits` ビットです — したがって `0x01,0x02` は既定幅では `0x0102` です。単一要素は
その値そのものです。

注意点。

- エントリパターンは通常のパターンなので、それ自身がキャプチャできます: `NZ::0` /
  `Z::0x08` を保持する `.sub::cc` があれば、`JMP!S{{cc}}c !e` は `jmpz 0x1234` に
  マッチします。
- 値リストはマッチの**後**に評価されるので、エントリパターンがキャプチャした変数を
  使えます: `R!n::n` はキャプチャした数字を束縛します。
- エントリはサブテーブルの中にのみ存在します。通常のパターン行としてマッチされること
  は決してありません。
- 参照が解決される前にパターンファイル全体が読まれるので、テーブルはその使用箇所より
  後に定義できます。未知のテーブル名と循環参照は、パターンファイルを読むときに一度
  報告されます。

**入れ子。** `.sub` ブロックを別の `.sub` ブロックの中に書くことは**できません**。
それはエラーです。入れ子は代わりに、エントリパターンが 2 つ目のテーブルを参照すること
で表現します。それは通常のパターン中の参照とまったく同じように展開されます:

```
LD!S{{outer}}a :: 0x10,a
.sub::outer
A!S{{inner}}b::b
B!S{{inner}}b::b+0x40
.return
.sub::inner
1::0x01
2::0x02
.return
```

```
lda1                 -> 10 01
ldb2                 -> 10 42
```

内側の変数が外側より先に束縛されるので、外側の値リストはそれを使えます — 上の例では
`b` が内側の値で、外側のエントリがそれに `0x40` を加えています。

参照の連鎖は最大 **8** 段まで展開されます。それより長い連鎖は展開されず、その行は単に
マッチしません。

#### 3.7.3 `.map` — シンボルテーブルとそのチェックを 1 行で

```
.map::<変数>::<名前,名前,...>::<変数を使った式>
.map::<変数>::<名前,名前,...>::<値,値,...>
```

リスト中の各名前に値を与え、変数をそのリストに制限します。値フィールドはどちらの形でも
読まれます。すべての値を計算する 1 つの式か、名前と 1 対 1 で対応する値のリストです。

式の中で変数は、**リスト中のその名前の位置**を表し、0 から数えます。

```
.map::x::R0,R1,R2,R3,R4::1<<x
```

は次と完全に等価です:

```
.setsym::R0::1<<(0)
.setsym::R1::1<<(1)
.setsym::R2::1<<(2)
.setsym::R3::1<<(3)
.setsym::R4::1<<(4)
.check::x::R0,R1,R2,R3,R4
```

式は省略でき、その場合は変数自身になるので、名前には順に 0, 1, 2, … が与えられます:

| 書き方 | `R0`…`R4` に与えられる値 |
|---|---|
| `.map::x::R0,R1,R2,R3,R4` | 0, 1, 2, 3, 4 |
| `.map::x::R0,R1,R2,R3,R4::x` | 0, 1, 2, 3, 4 |
| `.map::x::R0,R1,R2,R3,R4::1<<x` | 1, 2, 4, 8, 16 |
| `.map::x::R0,R1,R2,R3,R4::10**x` | 1, 10, 100, 1000, 10000 |

**値のリスト。** 値フィールドがカンマで区切られた複数の項目を保持する場合、項目は名前と
順に対応します:

```
.map::x::R0,R1,R2,R3::9,7,14,41
```

は次と完全に等価です:

```
.setsym::R0::9
.setsym::R1::7
.setsym::R2::14
.setsym::R3::41
.check::x::R0,R1,R2,R3
```

これは値に規則性のないテーブル向けの形式です。2 つのリストは同じ長さでなければならず、
そうでなければディレクティブはエラーを報告し何も定義しません。各項目は他と同じく式なので、
`.map::x::R0,R1,R2::x*10,100+x,7` は 0、101、7 を与えます — 変数は依然として位置を
表します。

ブラケットの外のカンマだけが項目を区切るので、自分のカンマを含む式は単一項目のままです:
`.map::x::R0,R1,R2::*(#W,x)` は 1 つの式のままで、名前には `W` の連続するバイトが与え
られます。

レジスタファイル、ビットマスク、その他名前*そのもの*が番号付けになっているテーブルが
よくあるケースで、`.setsym` 行を手で書き並べるとリストと値がずれやすくなります。`.map`
はパターンファイルを読むときに上記のディレクティブへ展開されるので、下流では特別扱い
されません。

位置は括弧に包んで式に代入されるので演算子の優先順位は影響を受けず、置換されるのは変数の
単語全体としての出現だけです — `0xff` の `x` はそのままです。

空要素 (`""`) はシンボルを定義せずに位置を消費するので、番号付けを揃えたまま、その
オペランドを `.check` リスト中で省略可能と印付けられます。値のリストと併用する場合は
その値も消費するので、2 つのリストは揃ったままです。

リストは配列シンボル（3.6.1 節）でもよいので、レジスタ名を一度書いて `.check` や
`.enum` と共有できます:

```
.setsym::regs::["R0","R1","R2","R3","R4"]
.map::x::regs::1<<x
```

**省略可能な位置。** `.check` リスト中の `""` はその位置が欠けていることを許します:

```
.setsym::a1::1
.setsym::a2::2
.setsym::a3::3
.setsym::b1::1
.setsym::b2::2
.setsym::b3::3
.setsym::c1::1
.setsym::c2::2
.setsym::c3::3
.check::a::a1,a2,a3,""
.check::b::b1,b2,b3,""
.check::c::c1,c2,c3,""
MOVabc:: ::a*100+b*10+c
```

```
mov                  0
mova1              100
mova1c3            103
movb2               20
movb2c1             21
```

AVX-512 のマスク記法も同じ仕組みを使います:

```
.symbolc::{}
.setsym::EAX::0
.setsym::EBX::1
.setsym::{K1}::1
.setsym::{K2}::2
.check::x::EAX,EBX
.check::k::{K1},{K2},""
FOO xk,y :: :: 0x90,k,x,y
```

```
FOO EAX,EBX          -> 0x90 0x00 0x00 0x01   (k 省略)
FOO EAX{K1},EBX      -> 0x90 0x01 0x00 0x01
FOO EAX{K2},EBX      -> 0x90 0x02 0x00 0x01
```

#### 3.7.4 `.free` — すべてのテーブルから名前を解放する

```
.free::x1,y1,z1
```

パターン層が保持するすべてのテーブルから各名前を削除するので、どのディレクティブが
定義したかを覚えていなくてもその名前を再利用できます。次のものをクリアします:

- その名前の `.setsym` 数値・文字列・配列・集合シンボル;
- その名前の `.sub` テーブル;
- `.check` リスト中の候補として現れるその名前;
- そして、その名前が変数名として読める場合、その変数の `.check` リスト全体、その
  `.enum`、およびその `.reloc` 宣言。

`.clearsym` や `.clrcheck` と同様に、`.free` は位置依存です。その上に書かれたパターン
はまだ名前を見ることができ、下のパターンは見られません。

```
.setsym::N1::7
.sub::T1
X::0x99
.return
BEFORE     :: :: #N1        /* 0x07 */
.free::N1,T1
AFTER      :: :: #N1        /* エラー - undefined symbol: '#N1' */
```

#### 3.7.5 `.reloc` — リロケーション型の宣言

```
.reloc::<変数>::<型名>
```

その変数が捕らえたラベル参照を、指定の ELF リロケーション型で `-o` の出力に書き
出します。`.check` と同じく位置依存で、後の `.reloc` が前のものを置き換え、
`.clrreloc::x`（引数なしなら全部）で解除します。`.free` も解除します。

型名は `-m` で選んだマシンの名前表 — ソース側で `::pc32` と書くときと同じもの
（8.3 節）— から引きます。

宣言が効くのは `-o` を指定したときだけです。指定がなければ以下の行は宣言が無い
のと全く同じに振る舞うので、1 組の行で両方の出力を兼ねられます。`-o` ならリンカ
に渡すリロケーション、`-b` なら axx が自分で出した答えです。

**なぜ必要か。** 通常、リロケーションの型と加数は出力バイト列から推定されます。
値が素の整数として連続バイトに並んでいるので、「出力された値 − ラベル値」が加数に
なるからです。ところが AArch64 の分岐やアドレス生成では、値は 32bit 命令語の中の
飛び飛びのビット欄に、しかも語単位・ページ単位に縮めて詰められます。この形からは
加数を逆算できず、幅だけを見た推定では `bl` に `PREL32` が当たってしまい、リンクは
通るのに 4 倍ずれた先へ分岐する、という壊れ方をします。

`.reloc` はその型を命令ごとに宣言します。加数はパターンが捕らえたオペランド値と
ラベル値の差になるので、`bl func` なら 0、`bl func+8` なら 8 です。命令語側の
ビット欄は 0 にして出力されます（RELA なのでリンカが埋めます。GNU as と同じ形）。

```
.reloc::t::call26
BL !t :: :: @@[4,(0x94000000|(((t-$$)>>2)&0x3ffffff))>>(%%*8)]
.clrreloc::t
```

```
bl func      ->  R_AARCH64_CALL26  func + 0
bl func+8    ->  R_AARCH64_CALL26  func + 8
```

**シンボル側では指定できない理由。** 型はシンボルではなくオペランドの位置で決まり
ます。AArch64 で 1 つのアドレスを作る `adrp` / `add` の対は、同じシンボルを
`ADR_PREL_PG_HI21` と `ADD_ABS_LO12_NC` という別々の型で参照します。`.extern
名前::型` のようなシンボル単位の指定では、原理的にこれを表せません。

```
.reloc::p::adrp
ADRP X!d,!p :: :: …
.clrreloc::p
.reloc::o::add_abs_lo12_nc
ADD X!d,X!n,#!o :: :: …
.clrreloc::o
```

```
adrp x0,msg      ->  R_AARCH64_ADR_PREL_PG_HI21  msg + 0
add  x0,x0,#msg  ->  R_AARCH64_ADD_ABS_LO12_NC   msg + 0
```

大きなパターンファイルではほぼ全行の被演算子変数が `t` なので、`.clrreloc` は
`.reloc` と同じだけ重要です。これが無いと、以降パターン走査が通り過ぎる全ての行に
宣言が効いたままになります。実例は `aarch64.axx` の Relocation modifiers 節で、
`:lo12:` `:pg_hi21:` `:abs_g*:` `:prel_g*:` `:got:` `:got_lo12:` のそれぞれに型を
宣言し、その行群の直後で解除しています。

1 回の実行で両方の出力を求めたときは注意が必要です。0 にした欄は生バイナリでも 0
のままなので、`-o` と並べて書いた `-b` の像は、そのオブジェクトをリンクした後で
ないと正しくありません。黙って通さず、axx が知らせます:

```
 warning - 16 instruction field(s) were left 0 for the linker (.text+0x0, ...);
 this raw binary is only correct after linking a.o. Drop -o to have axx fill
 them in.
```

AArch64 で使える命令フィールド型は次のとおりです。データ型（`abs64` `abs32`
`abs16` `pc64` `pc32` `pc16`）は従来どおり `.reloc` なしでも推定されます。

| 型名 | ELF | 欄 |
|---|---|---|
| `call26` / `jump26` | 283 / 282 | `bl` / `b` の imm26 |
| `condbr19` | 280 | `b.cond`、`cbz` の imm19 |
| `tstbr14` | 279 | `tbz` / `tbnz` の imm14 |
| `adr_prel_lo21` | 274 | `adr` の immlo/immhi |
| `adr_prel_pg_hi21` (`adrp`) | 275 | `adrp` の immlo/immhi |
| `adr_prel_pg_hi21_nc` | 276 | 同上、オーバーフロー検査なし |
| `add_abs_lo12_nc` | 277 | `add` の imm12 |
| `ldst8_abs_lo12_nc` | 278 | ロード/ストアの imm12 |
| `ldst16` / `ldst32` / `ldst64` / `ldst128_abs_lo12_nc` | 284 / 285 / 286 / 299 | 同上、幅ごとの縮尺 |
| `movw_uabs_g0` … `g3`（`_nc` 付きも） | 263–269 | `movz` / `movk` の imm16 |
| `movw_prel_g0` … `g3`（`_nc` 付きも） | 287–293 | 同上、PC 相対 |
| `got_page` (`adr_got_page`) | 311 | `adrp` の immlo/immhi — GOT ページ |
| `got_lo12` (`ld64_got_lo12_nc`) | 312 | 64bit `ldr` の imm12 — GOT 内オフセット |
| `got_ld_prel19` | 309 | リテラル `ldr` の imm19 |
| `ld64_gotpage_lo15` | 313 | imm12 |

GOT の 2 つは他と性質が違います。値はリンカが作る GOT エントリのアドレスなので、
アセンブル時には決まりません。欄は 0 のまま出て、意味はすべてリロケーションが
担います。

### 3.8 省略可能部分 (`[[ ]]`)

二重ブラケットは命令の省略可能な部分を示します:

```
INC (IX[[+!d]]) :: 0xdd,0x34,d
```

`inc (ix+0x12)` は `0xdd,0x34,0x12` を出力します。`inc (ix)` は `0xdd,0x34,0x00` を
出力します。小文字の変数は既定で 0 だからです。

### 3.9 パディング

```
.padding::0x12
```

アラインメントで使われるパディングバイトを設定します。既定は `0x00`。

### 3.10 8 ビット以外のワード幅 (`.bits`)

```
.bits::12
.bits::big::12
```

ビットスライスプロセッサや、ワードがバイトでないマシン向けです。既定は 8 ビットと
`little` です。

出力は常に 8 ビット単位なので、4 ビットマシンは 1 バイトあたり下位 4 ビットを出力し、
11 ビットマシンはエンディアンに応じて（下位 8、上位 3）または（上位 3、下位 8）を出力
します。バイト内の未使用ビットは 0 にマスクされます。

`.bits` が有効なとき、**ロケーションカウンタはバイトではなくワードを数えます**。
x86_64 のようなバイトアドレスのマシンでは `.bits` は不要です。

### 3.11 インクルード

```
.include "file.axx"
```

パターン側では、これはマクロ展開の*後*に処理されるので、マクロが `.include` 行自体を
生成できます。インクルードされた各ファイルは順にマクロ展開され、トップレベルのパターン
ファイルで定義されたマクロを継承します。

### 3.12 式中のエスケープ

式の評価はエスケープ文字 `\` で停止します。エスケープされた部分は先送りされ、パターン
ファイル内で再度処理されます。

```
LEAQ r, [ s + t * !h \+ !i ] :: 0x48,0x8d,0x04,((@h)-1)<<6|t<<3|s,i
```

は `leaq rax,[rax+rbx*(2+2)+0x40]` にマッチし、

```
LEAQ r,(s+t*!!h+!!i) :: 0x48,0x8d,0x04,((@h)-1)<<6|t<<3|s,i
```

は括弧形式 `leaq rax,(rax+rbx*(2+2)+0x40)` にマッチします。

### 3.13 負のインデックス変位

```
mov rax,[rbp-8]
```

は次にマッチします

```
MOV RAX,[RBP+!e]
```

パターンが `+` を持つ位置でソースに `-` が現れたとき、マッチャは両者を対応させ、
パターンの `+` をスキップし、`-` から始まる式を式評価器に渡します。パターンファイルに
特別な記法は必要ありません。

### 3.14 カスタムエラーメッセージ (`.error`)

```
.error::n::"メッセージ本文"
```

エラーコード `n` に対して表示される本文を登録します — `error_patterns` の条件が `;n`
で発生させるのと同じ `n` です（3.4 節）。6 つの組み込みコードのうち 2 つ（4、および
7 以上）はメッセージなしで出荷されます（9 節）。`.error` はそれらに本文を与え、既存の
コード（1、2、3、5、6）の本文を上書きすることもできます。`n` は非負の整数式でなければ
ならず、メッセージはダブルクォートで囲んだ文字列リテラルで、`.INCLUDE` のファイル名
（3.11 節）と同じ `\n` `\t` `\r` `\"` `\\` `\xHH` `\uHHHH` `\UHHHHHHHH` エスケープが
使えます。

```
.error::4::"このアドレッシングモードでは即値を表現できません。"
.error::9::"シフト量が範囲外です。"
ADD A,!v :: v<0||v>0xff;4 :: 0xc6,v
SHL A,!v :: v>7;9 :: 0xd0,v
```

`add a,300` は既定の空テキストではなく上記のメッセージでコード 4 を報告するように
なり、`shl a,9` は新しく定義されたコード 9 を報告します。`.setsym` と同様、同じ `n` に
対する後の `.error` は前のものを上書きしますが、`.setsym` と違ってこのテーブルには
領域依存の再定義という意味のある概念がないので、実際にはファイル先頭付近にコードごとに
1 つの `.error` を置くのが自然なスタイルです。メッセージが一度も与えられないコードも
依然としてエラーを発生させ、出力ファイルの書き出しをブロックします。今日の未宣言コード
とまったく同様に、印字されるテキストが空白のままになるだけです。

### 3.15 ミニ言語 (`.func` / `.call`)

`binary_list` の要素は `.call name(引数, ...)` にできます。これは小さな手続き型言語で
書かれた関数を実行し、その関数が生成するワードを出力します — `.emit` に渡した値と、
戻り値があればそれが続きます。この言語には代入、`.if`/`.elif`、`.while`、`.for`、再帰、
配列があるので、固定式として書けないエンコーディングを計算で求められます。`.echo` は
何も出力せずに標準エラー出力へ印字し、関数がなぜその結果を生んだかを調べるのに使えます。

関数はパターンファイルのトップレベルで定義します:

```
.func <name>(<引数, 引数, ...>)
<文>
.endfunc
```

ヘッダと対応する `.endfunc` の間のすべてが本体です。それらの行が通常のパターン行として
マッチされることはありません。引数は空でも構いません (`.func name()`、あるいは単に
`.func name`) — それを呼び出す `.call name(...)` と同じ形です。古い
`.func::name::params` ヘッダも依然として読まれます。`.func` の直後の `::` がそれを選ぶ
ので、既存のパターンファイルは動き続けます。

```
MOV a,!b,!c :: .call name(a,b,c)
```

呼び出し側の引数は通常の axx パターン式なので、そこでの `a`、`b`、`c` はキャプチャされた
パターン変数を意味します。関数の内側ではそれらが引数になり、その呼び出しに対してローカル
です。`.call` は他と同じくカンマ区切りの 1 要素なので、素の値と混在できます:

```
MIX !e :: 0x90,.call rep(e),0xff
```

`[e1, e2, ...]` と書かれた引数は**配列**です。その要素もパターン式です。`[]` は空配列です。

```
LOG !v :: .call table([0x11,0x22,v],3)
```

[3.5 節](#35-binary_list)の `;` 修飾子は `.call` にも適用されます。`;.call f(a)` は、
その呼び出しの出力全体が 0 に等しい単一ワードのとき何も出力しません。これは時々存在
しないプレフィックスバイトの書き方です。`;;.call f(a)` は呼び出しを実行してその出力を
捨てます。

#### 文

| 文 | 意味 |
|---|---|
| `name = expression` | ローカル変数への代入 |
| `name[index] = expression` | 配列要素への代入 |
| `.emit(e1, e2, ...)` | 値 1 つにつき 1 ワードを出力に追加 |
| `.echo(item, item, ...)` | 文字列と値を標準エラー出力へ印字 — デバッグ専用、何も出力しない |
| `.raise <expr>` | エラーコードが `<expr>` の値であるエラーを報告 |
| `.call name(args)` | 別の関数を呼ぶ |
| `name = .call name(args)` | 別の関数を呼び、その戻り値を代入 |
| `... .call name(args) ...` | 式の中の項としての呼び出し — その値は戻り値 |
| `.if <expr> .then` / `.elif <expr> .then` / `.else` / `.endif` | 条件分岐。`.elif` は繰り返せ、`.else` は省略可 |
| `.while(<expr>)` / `.endwhile` | 条件が非零の間ループ |
| `.for <name> in range(...)` / `.next` | `range(stop)`、`range(start, stop)`、`range(start, stop, step)` 上のループ |
| `.nonlocal a, b` | これらの名前をローカルではなく外側の呼び出しに束縛する |
| `.return` | 関数から戻る。本体のどこにでも（トップレベルでも `.if`/`.while`/`.for` の中でも）、何度でも書ける |
| `.return <expr>` | 関数から値を返す |
| `.endfunc` | 関数本体を閉じる（上記参照） |

`.emit` は値 1 つにつき `.bits` 幅のワードを 1 つ追加します — 既定幅なら 1 バイトです。
呼び出しが出力するワード数が命令の長さになるので、アドレスが収束するためには関数が
両方のパスで同じ個数を出力しなければなりません。

`.if` と `.elif` の行は `.then` で終わらなければなりません。`.if` の後に任意個の
`.elif` 分岐が続けられ、`.else` が連鎖を閉じ、1 つの `.endif` が連鎖全体を終えます:

```
.if n > 100 .then
.emit(0xff, n & 0xff)
.elif n > 10 .then
.emit(0xfe, n & 0xff)
.else
.emit(n & 0xff)
.endif
```

`.echo` は `.emit` のデバッグ版です。標準エラー出力へ書き、出力には何も追加しないので、
これを足したり消したりしても生成バイトが変わることはありません。各項目は `"..."` 文字列
リテラルか式で、両者を混在できます:

```
.echo("Example", n, 1)          /* -> Example 7 1 */
.echo("n=", n, "arr=", a)       /* -> n= 7 arr= [7, 14] */
```

文字列は書いたとおりに、整数は符号付き 10 進で、配列は `[1, 2, 3]` として印字され、
1 回の呼び出しの項目はスペース区切りで 1 行を共有します — マクロ層の `!echo` が使うのと
同じ出力ルーチンなので、両者は一致します。項目なしの `.echo()` は空行を印字します。
アセンブラが命令長を測定している間やパス 1 で収束している間は何も印字されないので、
各行はリラクゼーションの反復ごとではなく、アセンブルされた命令ごとに一度現れます。

文字列は `.echo` の引数リストの中にのみ現れられます。値は整数と配列だけなので、文字列を
変数に代入したり式の中で使ったりはできません。エスケープは `\\`、`\"`、`\n`、`\t` で、
それ以外の `\` はエラーです。文字列に `/*` を含めることはできません — それはパターン
ファイルのコメントマーカーで、ミニ言語が行を見る前に除去されます。

`.raise <expr>` は、エラーコードが `<expr>` の値であるエラーを報告します。`error_patterns`
フィールドが `condition;code` に対して生成するのとまったく同じ形式です
（[3.4 節](#34-error_patterns)）。`.error::code::"text"` で登録されたメッセージも同じ
ように使われ、未登録のコードは番号だけを印字します。報告は関数を止めません — 実行は次の
文へ続くので、そこで止めたければ後に `.return` を置いてください — そして `.echo` と同様、
命令長を測定しているだけの間とパス 1 のリラクゼーション中は静かにしているので、アセンブル
された各命令は多くとも一度だけ報告します。発火した `error_patterns` と同様に、アセンブルは
失敗し出力ファイルは書かれません。

```
.error::3::"immediate out of range"

MOV !n :: .call mov(n)

.func mov(n)
.if n > 255 .then
.raise 3
.return
.endif
.emit(0xb0, n)
.return
.endfunc
```

値は 256 ビット 2 の補数整数で、axx の他のあらゆる場所と同じです。演算子は、緩いものから
きついものへ: `||`、`&&`、`!`、比較 (`== != < <= > >=`)、`|`、`^`、`&`、`<< >>`、
`+ -`、`* / %`、単項 `- + ~`、`**`、添字。`/` はゼロ方向へ切り捨て、`%` は被除数の符号を
取ります。`>>` は算術シフトです。

4 つの項は代わりにアセンブラ自身の式評価器に渡されます: `$$`、`$.`、`#name`、および
どのローカル変数も主張しない裸の名前（ラベルまたは `.equ` として読まれます）。双方とも
256 ビットなので、値はそのまま使える形で返ってきます。

```
BR !t :: .call rel8(t)

.func rel8(target)
d = target - $.
.if d < -128 || d > 127 .then
.echo("branch out of range:", d)
.endif
.emit(d & 0xff)
.return
.endfunc
```

パターン変数と `!!!` はそこでは*利用できません*。`.func` 本体の実行中はそれらを束縛
しているものが何もないからです。代わりに呼び出し側で `.call f(a,b)` として渡してください。
[6.3 節](#63-どこで何が使えるか)に完全な表があります。未定義ラベルから導かれた値は 0
として渡され、`.call` の引数と同じ扱いなので、巨大なセンチネル値がループ回数を吹き飛ばす
ことはありません。

#### 戻り値

`.return <expr>` は値を返し、呼び出し側は `var = .call name(args)` でそれを受け取ります:

```
.func hypot2(a,b)
.return a*a+b*b
.endfunc

.func emit_h(a,b)
v = .call hypot2(a,b)
.emit(v)
.return
.endfunc
```

値は数値でも配列でもよく、配列はコピーとして渡されます。代入先は配列要素でもよく
(`a[i] = .call f(x)`)、その場合返される値は数値でなければなりません。何も返さない関数を
この形で呼ぶのはエラーです。

`.call name(args)` はより大きな式の*中の*項としても現れられ、そこでは関数が返す値を
表します:

```
d = .call hypot2(a,b) + .call hypot2(c,e)
.if(.call firstdiv(n) == n)
m = t[.call idx(k)]
```

呼び出しは別の呼び出しの引数の中に入れ子にでき、その引数は通常の式です。何も返さない
関数はこの形では使えません — 代入形式と同様にエラーです。`.return` は本体を閉じません —
それをするのは `.endfunc` だけです — ので、`.return <expr>` はブロックの内側からの
早期リターンとしても、何度でも使えます:

```
.func firstdiv(n)
i=2
.while(i<n)
.if n%i==0 .then
.return i
.endif
i=i+1
.endwhile
.return 0
.endfunc
```

関数が `binary_list` から直接呼ばれたとき、その戻り値は出力になります。数値は 1 ワード、
配列はインデックス 0 から要素 1 つにつき 1 ワードです。これは `.emit` に渡したものに
加えてなので、`.emit` するだけで何も返さない関数は従来とまったく同じ振る舞いをします。

```
SEQ !n :: 0xaa,.call seq(n),0xbb

.func seq(n)
a=[]
.for i in range(n)
a[i]=0xc0+i
.next
.return a
.endfunc
```

```
seq 4                -> aa c0 c1 c2 c3 bb
```

#### 配列

配列は `[]`、`[e1, e2, ...]` と書くか、スライスで生成します。配列は関数の引数としても
渡せます。関数間でも、`binary_list` の呼び出し側からでも可能です。

```
a = []
a[3] = 5          /* a は [0,0,0,5] になる — 隙間は 0 で埋められる */
.emit(a[0])       /* 0 */
.emit(a[99])      /* 0 — 末尾を超えた読み出しは 0 を返し、拡張しない */
.emit(.len(a))    /* 4 */
b = a[1:3]        /* [0,0] — 終端インデックスは含まれない */
```

末尾を超えた代入は配列をゼロで拡張します。末尾を超えた読み出しや負のインデックスでの
読み出しは `0` を返し、配列はそのままです。スライスの境界は配列にクランプされます。
`.len(x)` は長さです。配列要素は数値であり、配列ではありません。

#### スコープと入れ子

各呼び出しは自分の変数を持ちます。設定される前に読まれた名前はエラーなので、タイプミスが
黙ってゼロを読むことはありません。

関数は他の関数の中で定義できます。内側の関数は外側の関数から見え、名前を外向きに解決
します。`.nonlocal` を使うと、自分のものを作る代わりに外側の呼び出しの変数へ代入でき
ます:

```
.func outer()
n=7
.func inner()
.nonlocal n
n=n+1
.emit(n)
.return
.endfunc
.call inner()
.call inner()
.emit(n)
.return
.endfunc
```

```
-> 08 09 09
```

`.nonlocal` は、その関数内でその名前が他の用途で使われる前に現れなければなりません。

#### 例

篩（ふるい）と、エンコーディングが Collatz のステップ数である命令:

```
PRIMES !n  :: .call sieve(n)
COLLATZ !n :: .call collatz(n)

.func sieve(n)
mark=[]
mark[n]=0
i=2
.while(i*i<n)
.if mark[i]==0 .then
j=i*i
.while(j<n)
mark[j]=1
j=j+i
.endwhile
.endif
i=i+1
.endwhile
.for k in range(2,n)
.if mark[k]==0 .then
.emit(k)
.endif
.next
.return
.endfunc

.func collatz(n)
c=0
.while(n!=1)
.if n%2==0 .then
n=n/2
.else
n=3*n+1
.endif
c=c+1
.endwhile
.emit(c)
.return
.endfunc
```

```
primes 50            -> 02 03 05 07 0b 0d 11 13 17 1d 1f 25 29 2b 2f
collatz 27           -> 6f
```

同じ内容は `MINI.md`（日本語）と `mini_en.md`（英語）に独立した文書として保管されて
います。

#### 実在 ISA での実践例

`aarch64_logical_mini.axx` は AArch64 の論理即値グループ（AND / ORR / EOR / ANDS / TST、
32 ビットと 64 ビット）をこの方法でエンコードします。ARM の `DecodeBitMasks` を逆向きに
実行するものが 4 つの関数 — `highbit`、`find_e`、`pack_rs`、`emit_logical` — に分割
されており、おかげで各命令行は短く済んでいます:

```
AND  d,n,#!v ::v==0;3,v==0xFFFFFFFFFFFFFFFF;3 :: .call emit_logical(v,1,0,n,d,64)
```

[付録 A.3](#a3-aarch64-論理即値)は同じエンコーディングを、単一の連鎖三項演算子による
`binary_list` として書いたものを示します。ミニ言語はまさにそれを置き換えるために
あります。どちらも同じ 256 ビット演算で計算します。関数形式が得るのは、各呼び出しが
自分のローカルスコープを持つのでその作業変数がパターン層のキャプチャ変数と衝突しない
こと、そして 1 つの本体が両方の幅に対応できることです — 32 ビット形式は要素サイズの
探索を 64 ではなく 32 に制限するだけですが、`binary_list` 版では幅を式の中に折り込む
必要があります。

#### 制限

この言語はチューリング完全なので、バグのあるパターンファイルがそのままではアセンブラを
ハングさせかねません。4 つの上限がそれを防ぎ、代わりに問題の行を報告します:

| 上限 | 値 |
|---|---|
| `.call` ごとに実行される文の数 | 4,000,000 |
| 呼び出しの入れ子 | 128 |
| `.call` ごとに出力されるワード数 | 1,048,576 |
| 配列長 | 1,048,576 |

未定義ラベルから来た引数は `0` として渡されるので、前方参照が最初のパスでループ回数を
吹き飛ばすことはありません。

### 3.16 `.passthru` — マッチしない行の素通し

既定では、どのパターンにもマッチしないソース行は `Syntax error`（コード1）になり、
出力は破棄されます。`.passthru` はその行を**エラーにせずそのままテキストとして出す**
ように切り替えます。トランスレータとして使うとき（3.5.2 節のテキストテンプレート）、
書き換えたい行だけをパターンに書き、残りを通したい場合のためのものです。

```
.passthru          /* on と同じ */
.passthru::on      /* 素通しする */
.passthru::off     /* 素通しをやめる（既定） */
```

行末の改行はこのディレクティブの仕事ではありません。1行が1行になるようにしたいときは
`.eol`（3.17 節）を併せて書きます。

素通しされた行は、エンコーディング欄が `"<その行>"` というテキストテンプレートだった
パターンにマッチしたのと同じ扱いになります。つまり UTF-8 の 1 バイトが 1 ワードとして
出力され、ロケーションカウンタもその分進み、標準出力にもテキストとして出ます。両方の
パスで同じテキストを出すので、素通しした行のあとのラベルも正しいアドレスになります。

```
.bits::8
.passthru
.eol
.map::r::AX,BX,CX
.setsym::rn::[AX,BX,CX]
MOV r,!e::"LD {{rn[r]}},0x{{.hex(e)}}"
```

```
	mov	bx,5        ->  LD BX,0x5
	xyzzy	foo, bar    ->  xyzzy foo, bar     /* 素通し */
	r1 = r2 + r3        ->  r1 = r2 + r3       /* 素通し */
```

出るのは**照合にかけた形の行**です。タブや連続した空白は 1 つの空白に詰められ、`;` から
始まるコメントと行頭のラベル定義は落ちています（ラベルはラベルとして登録されます）。

パターンファイルは 1 行ごとに全部走査されるので、`.passthru` はファイル全体にかかる設定
として働きます（同じファイルに複数書いた場合は最後のものが効きます）。

`.ascii` や `.resb` のような組み込みのアセンブリディレクティブ（5.3 節）は素通しより先に
処理されるので、そのままの意味で働きます。一方、`.aling` のような**綴り違いは素通しされ
ます** — 素通しを有効にすると、書き損じがエラーにならずそのまま出力に流れることに
注意してください。

### 3.17 `.eol` — 行ごとに改行を入れる

テキスト変換のための設定です。**出力を出した行ごとに改行（`\n`）を 1 ワード足します**。
テキストテンプレートの一つ一つに `\n` を書いて回らなくても、ソースの 1 行が出力の 1 行に
なります。

```
.eol          /* on と同じ */
.eol::on      /* 行ごとに改行を足す */
.eol::off     /* 足さない（既定） */
```

```
.bits::8
.eol
.map::r::AX,BX,CX
.setsym::rn::[AX,BX,CX]
MOV r,!e::"LD {{rn[r]}},0x{{.hex(e)}}"
NOP::"NOP"
```

```
	mov	ax,0x10     ->  LD AX,0x10\n
	nop                 ->  NOP\n
```

- 足すのは**出力ワード列の側だけ**です。標準出力へ流すテキスト（トランスレータとしての
  出力）には足しません — そちらは行ごとに改行して出しているので、二重に改行しない
  ようにしてあります。つまり `-b`／`-o` のファイルには改行が入り、標準出力の見た目は
  変わりません。
- 出力ワードを 1 つも出さなかった行には足しません（コメントだけの行、何も出さない
  パターン、`.section` のようなディレクティブ行）。`.ascii` などが直接書き出すバイト
  （5.3 節）にも足しません。
- `.vliw` が有効なときは、パケットを壊さないよう何もしません。
- `.passthru`（3.16 節）で素通しした行にも、同じように改行が入ります。

パターンファイルは 1 行ごとに全部走査されるので、これはファイル全体にかかる設定として
働きます（同じファイルに複数書いた場合は最後のものが効きます）。


---

## 4. VLIW と EPIC プロセッサ

### 4.1 `.vliw`

```
.vliw::<バンドルビット数>::<命令ビット数>::<テンプレートビット数>::<NOP コード>
.vliw::128::41::5::00
```

この例は Itanium を記述しています。128 ビットのバンドルが 41 ビットの命令 3 つ
（123 ビット）と 5 ビットのテンプレートを保持し、NOP は `0x00` です。

- 非 EPIC マシンでは、テンプレートビット数に `0` を与えます。
- テンプレートビット数が正ならテンプレートを右端に、負なら絶対値を幅として左端に
  配置します。
- `.bits::big` は既定の `little` に対して出力のバイト順を逆にします。

パターンが出力するバイト数は `(バンドルビット数 − テンプレートビット数) ÷ 8` を
切り上げた値と一致しなければなりません。

### 4.2 EPIC

EPIC パターンは**第 4 の**フィールド、インデックスコードを取ります。

```
/* VLIW
.setsym::R1::1
.setsym::R2::2
.setsym::R3::3
.setsym::R4::4
.vliw::128::41::5::00
EPIC::1,2::0x8|!!!!
EPIC::1::0x01
AD a,b,c:: ::0x01,0,0,a,b,c::1
LOD d,[!e]:: :: 0x00,0x01,0,d,e,e>>8::2
```

- `EPIC::1,2::0x8|!!!!` は、インデックスコード 1 と 2 の命令からなるバンドルを、
  テンプレート `0x8` とストップビットの OR で記述します。
- `!!!!` はストップビットを表します。
- `AD a,b,c` のインデックスコードは 1、`LOD d,[!e]` は 2 です。

**EPIC パターンではエラーフィールドを明示的に書かなければなりません。** 空のときも
同様です — だから `:: ::` となっています。

（上記の値はテストフィクスチャであり、実際の Itanium エンコーディングとは対応して
いません。）

### 4.3 非 EPIC VLIW

```
/* VLIW
.setsym::R1::1
.setsym::R2::2
.setsym::R3::3
.setsym::R4::4
.vliw::128::32::0::0x00
AD a,b,c::0x01,a,b,c
LOD d,[!e]::0x02,d,e,e>>8
JMP !a ::0x03,a,a>>8,0
```

### 4.4 ソース中でのバンドル化

命令は `!!` でバンドル化します:

```
ad r1,r2,r3 !! lod r4,[0x1234]
```

`binary_list` では `!!!` が `!!` で結合された命令の数であり、バンドル末尾の `!!!!` が
ストップビットを設定します。

### 4.5 エンディアン

`binary_list` に値を書く順序で決まります。

---

## 5. アセンブリソースリファレンス

ソースファイルまたは標準入力から読まれる行を**アセンブリ行**と呼びます。コメントは
`;` で始まります。

### 5.1 ラベル

```
label1:
label2: .equ 0x10
label3: nop
label4: .equ label1
```

ラベルは文字・数字・一部の記号の並びで、数字以外で始まります。`.equ` で定義された
ラベルは再配置情報を失い、定数として扱われます。

`.labelc::<characters>` はラベルの文字集合を拡張します。既定は文字、数字、`_`、`.` です。

### 5.2 ロケーションカウンタ

```
.org 0x800
.org 0x800,p
```

`.org` はロケーションカウンタを設定します。`,p` を付けると、カウンタが目標値より下に
ある場合、その隙間がパディングされます。

```
.align 16
```

16 の倍数に整列し、`.padding` バイトでパディングします。引数なしの場合、前回の（または
既定の）アラインメントが使われます。

### 5.3 データ

```
.ascii "sample1"        ; 文字列バイト
.asciz "sample2"        ; 文字列バイトと末尾の 0x00
.zero 65536             ; 65536 個のゼロバイト
```

バイトを出力せずに記憶領域を予約する — ロケーションカウンタが進むだけです:

```
.resb n     ; n バイト
.resw n     ; n ワード       (n*2 バイト)
.resd n     ; n ダブルワード (n*4 バイト)
.resq n     ; n クワッドワード (n*8 バイト)
```

**`DB` のようなバイト出力ニーモニックは axx に組み込まれていません。** パターンファイル
が定義した場合にのみ存在します。同梱ファイルのうち `8048.axx` と `x86_64.axx` は `DB` を
定義しており、`z80.axx` は定義していません。パターンファイルに関係なく常に使えるデータ
ディレクティブは `.ascii`、`.asciz`、`.zero`、および `.resb`/`.resw`/`.resd`/`.resq`
ファミリです。

### 5.4 浮動小数点

`!F` / `!D` / `!Q` は命令中のある位置で浮動小数点オペランドをキャプチャします:

```
VMOV.F32 S!n,#!Fd :: 0x80|n,d>>24,d>>16,d>>8,d
```

`vmov.f32 s0,#3.14` は `0x80,0xc3,0xf5,0x48,0x40` を出力します。

通常の整数式の中に浮動小数点値を書くには — たとえば `.equ` で名前を与えるには — 波括弧
形式を使います。それぞれ本体を浮動小数点として評価し、IEEE-754 ビットパターンを整数と
して返します:

```
flt{expr}     ; 32 ビット  (binary32) ビットパターン
dbl{expr}     ; 64 ビット  (binary64) ビットパターン
qad{expr}     ; 128 ビット (binary128) ビットパターン
```

逆関数はそれらの本体の内側で使えます:

```
enfloat(v)  / enflt(v)     ; v の下位 32 ビットを float として読み戻す
endouble(v) / endbl(v)     ; v の下位 64 ビットを double として読み戻す
```

これにより名前付きの浮動小数点定数が機能します。格納されたラベルがデコードして再計算
できるビットパターンを保持しているからです:

```
c1: .equ flt{3.14}
    LDF A,flt{enfloat(:c1)*2}
```

そのような式の中の `:label` は、ラベルの値を直接参照します。`inf`、`-inf`、`nan` が
受け付けられます。2 進リテラルには `0b`、16 進には `0x` を使います。

### 5.5 セクション

```
.section .text
.segment .text          ; 現在は意味が同一
.endsection
.endsegment
```

セクション名は、`-o` と `-E` 出力用の ELF セクションフラグを導く際に `.text`、`.data`、
`.rodata`、`.bss` と照合されます。

> **注意。** `.section`（または `.segment`）がセクションを切り替える唯一の方法です。
> 素の `.text` / `.data` / `.rodata` / `.bss` という短縮形はありません。それらを単独の
> 行に書くと、Paxx でも Caxx でも構文エラーです。

#### 5.5.1 `.reloctype`

現在のソースファイルにおける、自動検出されたラベル参照に対するマシン既定の幅推測
再配置タイプを上書きします:

```
.reloctype name8,name16,name32,name64
```

#### 5.5.2 セクションの順序

セクションは書かれた順序どおりに配置されるので、次のコードは:

```
.section .text
ld a,9
.section .data
.asciz "test1"
.section .text
ld b,9
.section .data
.ascii "test2"
```

セクションごとにまとまりません。`secsort.py` を実行すると次が得られます:

```
.section .text
ld a,9
ld b,9
.section .data
.asciz "test1"
.ascii "test2"
```

### 5.6 リンケージ

```
.export label
.export label1,label2,label3
.global label1,label2
.extern label1,label2
.extern label1:2,label2          ; label1 は再配置タイプ 2 を使う
```

- `.export` はラベルを `-e` / `-E` 出力の対象として、そのセクション/セグメントとともに
  マークします。ここで名指しされたラベルだけがエクスポートされます。
- `.global` はラベルを外部へ渡します。`-e` / `-E` でも書き出されます。
- `.extern` はその名前が他所で解決されることを宣言します。再配置タイプは `:` で個々の
  名前に付けられます。

`.extern` と `-i` は一緒に使うよう設計されています。`-i` が外部ラベルの実アドレスを
供給し、`.extern` がその名前が他所で解決されることを宣言します。両方が同じラベルを
名指しした場合、`-i` が持ち込んだ値が優先されます。

`.global` と `.extern` は ELF オブジェクトライタが消費します。

### 5.7 インクルード

```
.include "file.s"
```

ソース側ではこれは**マクロ層をバイパスします**。マクロ定義を取り込むには `!include`
（7 節）を使ってください。

---

## 6. 式と演算子

アセンブリ行とパターンデータは同じ式評価器を呼ぶので、両者はほぼ同一に振る舞います。
唯一の制限は、**小文字のパターン変数をアセンブリ行から参照できない**ことです。

### 6.1 特殊な項

| 項 | 意味 |
|---|---|
| `!!!` | `!!` で結合された命令の数 |
| `%%` | `%%` がこれまでに現れた回数（0 起点のインデックス） |
| `$$` | 現在のロケーションカウンタ |
| `$.` | 次の命令の開始アドレス |

### 6.2 演算子

優先順位は Python に従い、緩いものが後です:

```
(expr)          括弧で囲んだ式
#               後続シンボルの値
*(x,y)          x の最下位から y 番目のバイト (y>=0)
-, ~            符号反転、ビット NOT
@               最上位の立っているビットの位置。右から数える
'c'             文字コード
:=              代入
**              べき乗
*, /, //        乗算、除算、整数除算
+, -            加算、減算
<<, >>          シフト
&               ビット AND
|               ビット OR
^               ビット XOR
'               符号拡張
<=, <, >, >=, !=, ==     比較
not(x)          論理 NOT
&&              論理 AND
||              論理 OR
x?a:b           三項演算子
```

- `d:=24` は `d` に 24 を代入し、24 に評価されます。
- `#name` はシンボル `name` の値を返します。
- `@v` は `v` の最上位の立っているビットの位置を右から数えて返します。
  （ヘビマルマッタ演算子。）
- `a'24` は `a` を符号拡張し、ビット 24 を符号ビットとして扱います。（SEX 演算子。）

### 6.3 どこで何が使えるか

同じ評価器が 3 か所から呼ばれます — アセンブリ行、パターンファイルのフィールド、そして
（委譲によって）マクロ層とミニ言語です — が、すべての項がすべての場所で意味を持つわけ
ではありません。ケイパビリティ記述子が決定するので、機能セットは呼び出しの時点によって
変わります:

| 項 | パターン行 | アセンブリ行 | ミニ言語 | マクロ層 |
|---|---|---|---|---|
| ラベル / `.equ` | 可 | 可 | 可 | 前回の反復の値 |
| `$$` / `$.` | 可 | 可 | 可 | `$` / `$$`、前回の反復 |
| `#symbol` | 可 | 可 | 可 | — |
| パターン変数 | 可 | 大文字のみ | 不可 | 不可 |
| `!!!` / `!!!!` | 可 | 不可 | 不可 | 不可 |
| `@`, `'`, `*(x,y)` | 可 | 可 | 可 | 可 |

ミニ言語でパターン変数が外されているのは、`.func` 本体の実行中にそれらを束縛している
ものが何もないからです — 代わりに呼び出し側で渡してください。`!!!` は VLIW パターン行
でしか意味を持ちません。パターンファイルではマクロ層がソースのアセンブル前に走るので、
読むべきラベルもロケーションカウンタもまったく存在しません。

`@`、`'`、`*(x,y)` は一度だけ実装され、アセンブラの評価器とマクロ層の両方から呼ばれる
ので、どちらでも同じ意味です。マクロ層では `'` はビット演算子より緩く、`&&` よりきつく
結合します。この位置はアセンブラでの位置（`^` と比較の間）とは異なります。マクロ層の
優先順位は C に従い、そこでは比較がビット演算子よりきつく結合するからです。

### 6.4 非常に大きな値と UNDEF センチネル

未定義ラベルは通常の算術を通してセンチネル値を運ぶので、`label+4` は例外を発生させる
代わりに未定義と認識できる形を保ちます。Paxx はそのセンチネルを帯域外に置けます —
その整数は任意精度なので、センチネルは実際の値が到達しない `2**768` より上に存在します。
Caxx には 256 ビットより上の余裕がないので、`2**192` 以上の任意の大きさをセンチネル由来
として扱い、警告で一度そう述べます。

計算値を `2**192` より下に保てば、2 つの実装は完全に一致します。それを超えると、
それぞれ独自の道を行きます:

| 値 | Paxx | Caxx |
|---|---|---|
| 絶対値 ≥ `2**192` | 通常の値 | 未定義ラベル参照として報告 |
| ビット 255 が立つ | 正なので `>>` はゼロを詰める | 負なので `>>` は 1 を詰める |
| ≥ `2**256` | そのまま保持 | 256 ビットに巻き戻す |

両実装とも、値が判断できない帯域に入ったとき警告します。これはセンチネル設計の性質で
あって、特定の演算子の性質ではありません。Caxx に帯域外センチネルを持たせるには、値型
を 256 ビットより広げる必要があります。

---

## 7. マクロ層

同じ内容は `MACRO.md`（日本語）と `macro_en.md`（英語）に独立した文書として保管されて
います。

これはアセンブラ本体の**前**に走るソース間変換です。それでもソースファイル中のマクロは
ラベル値、`.equ` 定義、`$` / `$$` ロケーションカウンタを読めます。見えるのは*前回の*
リラクゼーション反復の値です（最初の反復では 0 / 未定義）。展開が反復ごとに変わりうる
ため、収束はリラクゼーションループ自体によって強制されます — 反復回数に上限を設け、
振動を検出し、誤ったバイナリを出力する代わりに出力ファイルを書かずに中断します。
パターンファイルのマクロはソースのアセンブル前に走るので、どちらにもアクセスできません。
詳細は `MACRO.md` / `macro_en.md` を参照してください。

`axx.py` と `caxx.c` は同じ仕様を実装しています。唯一の違いは数値表現です。Paxx は任意
精度整数を、Caxx は `int64` を使います。結果が食い違うのはマクロ時の計算が 64 ビットを
超える場合だけで、マクロ層はソーステキストを出力するので、アセンブラ自身の 256 ビット
式評価には影響しません。

### 7.1 文

すべての文は行頭の `!` で始まります（先頭の空白は無視されます）。

| 構文 | 意味 |
|---|---|
| `!def name(p1, p2, p3 = default) { ... }` | マクロ / コンパイル時関数 |
| `!return expr` | 値を返す。早期脱出も兼ねる |
| `!if expr !then { ... } !elif expr !then { ... } !else { ... }` | 条件分岐 |
| `!while expr { ... }` | ループ |
| `!break` / `!continue` | ループ制御 |
| `!set name = expr` | 代入。内側から外側へスコープを探索し、見つからなければ現在のスコープに作成 |
| `!local name [= expr]` | 現在のスコープで宣言 |
| `!undef name` | 変数またはマクロを削除 |
| `!name(a, b)` | マクロを文として展開 |
| `!include "file"` | 展開時にテキストをインクルード |
| `!error expr` | エラーで展開を中断 |
| `!warning expr` / `!echo expr` | 標準エラー出力へ書く（`!echo` はミニ言語の `.echo` とレイアウトを共有。[3.15 節](#315-ミニ言語-func--call)） |

開き `{` はヘッダ行の最後になければならず、閉じ `}` は行頭になければなりません。
`; コメント` は文の後に続けられます。

### 7.2 補間

| 記法 | 意味 |
|---|---|
| `!{expr}` | 値をテキストとして展開 |
| `!{expr:04x}` | Python 形式のフォーマット指定を適用 |
| `\!{` | リテラルな `!{` |

フォーマット指定は Python のフォーマットミニ言語です。両実装は同じ指定を受け入れ、
同じ指定を拒否し、エラーの文面も一致します。

```
[[fill]align][sign][z][#][0][width][grouping][.precision][type]
  align     ::= "<" | ">" | "=" | "^"
  sign      ::= "+" | "-" | " "
  grouping  ::= "," | "_"
  type      ::= b c d e E f F g G n o s x X %
```

`!{255:#06x}` は `0x00ff`、`!{1234567:,d}` は `1,234,567`、`!{255:*^9b}` は
`*11111111*`、`!{255:.2f}` は `255.00` を与えます。Python の制約も適用されます。整数型
に精度は指定できず、`x`/`X`/`o`/`b`/`c`/`n` に `,` は使えず、文字列に符号や `#` は
付けられません。

Python との既知の相違が 2 つあります:

- 両実装とも解釈前に指定の前後の空白を除去するので、空白を符号とする形式（`!{5: d}`、
  Python では `' 5'` になる）は表現できません。`!{5:d}` と同じに振る舞います。
- `!{0:c}` は Paxx では NUL 文字を返しますが、Caxx では空文字列になります。C 文字列は
  内部に NUL を持てないためです。

### 7.3 値、演算子、組み込み関数

値は整数と文字列です。演算子は C 準拠です:

```
?:  ||  &&  |  ^  &  ==  !=  <  <=  >  >=  <<  >>  +  -  *  /  %
単項:  -  +  ~  !
```

`/` と `%` は C と同様にゼロ方向へ切り捨てます (`-7/2 == -3`)。どちらかのオペランドが
文字列なら `+` は連結し、`"ab" * 3` は繰り返します。整数リテラル: `10`、`0x1f`、
`0b1010`、`0o17`。アンダースコアが使えます。`'A'` は 1 文字なら文字コード、それ以上なら
文字列です。

さらに 3 つがアセンブラ自身の評価器から持ち込まれ、実装を共有しているのでどちらの場所
でも同じ意味を持ちます: `@v`（最上位の立っているビットの位置）、`v'bits`（符号拡張）、
`*(v, n)`（バイト抽出）です — `!{@255}` は 8、`!{0xff'8}` は -1、`!{*(0xabcd,1)}` は
171 です。`'` はビット演算子より緩く、`&&` よりきつく結合します。位置がアセンブラと
異なる理由は [6.3 節](#63-どこで何が使えるか)を参照してください。値が期待される位置の
`*(` だけがバイト抽出です。中置の `*` は依然として乗算です。

組み込み関数:

```
len(s)  hex(v[,digits])  str(v)  int(s[,base])  upper(s)  lower(s)
substr(s,start[,length])  abs(v)  min(...)  max(...)  uid()  label(name)
defined(name)
```

`substr()` は開始位置と長さを文字列にクランプします。負の開始位置は先頭 (0) を意味し、
Python 風の末尾からのインデックスでは**ありません**。負の長さは 0 として扱われます。

`label(name)` は、`label(".L1")` のようにマクロ識別子として書けない名前について、
アセンブラ側のラベルまたは `.equ` の値を読みます。有効な識別子*である*名前は裸で書いて
読みます — 裸の識別子はまずマクロ変数として解決され、変数が見つからない場合にのみラベル
として解決されます。どちらの形式もソースファイルでのみ機能します。パターンファイルでは
まだラベルが存在しないので、裸の識別子はマクロ変数としてのみ解決されます。どちらの形式
が読むのも、この節の冒頭で述べたとおり前回のリラクゼーション反復の値です。

マクロ内の暗黙変数:

| 名前 | 内容 |
|---|---|
| `__id__` | 呼び出しごとに一意な整数 — ローカルラベル生成用 |
| `__name__` | マクロ名 |

### 7.4 パターンファイル中のマクロ

パターンファイルも同じマクロ層を通ります。命令テーブルはレジスタ番号やオペコードだけが
異なる同形の行の並びになりがちなので、その行を生成できます:

```
!def alu(name, base) {          /* 文の行では、パターンファイルのコメント構文
!local r = 0
!while r < 8 {
!{name} A,R!{r} :: 0x!{base + r:02x}
!set r = r + 1
}
}
!alu("ADD", 0x80)
!alu("SUB", 0x90)
```

エラーフィールドも生成できるので、一度書いた範囲チェックが生成されたすべての行に適用
されます:

```
!def imm(name, op) {
!{name} A,!v :: v>0xff;2,v<0;2 :: 0x!{op:02x},v
}
!imm("ADDI", 0xc6)
!imm("SUBI", 0xd6)
```

ソース側と異なる点が 3 つあります。構文、組み込み関数、暴走防止は同一です。

1. **独立した名前空間。** パターン側とソース側は独立したマクロ環境を持ち、互いのマクロ
   や変数を見ることはできません。したがってパターンファイルのマクロがソースファイルの
   展開のされ方を変えることは決してなく、リラクゼーション中にソース側が行うパスごとの
   リセットが、パターンファイルの読み込み中に定義されたマクロを消し去ることも決して
   ありません。
2. **文の行のコメントは `/*` を使い、** パターンファイルの慣習に合わせます。そこでは
   `;` はコメントマーカーではありません。エラーフィールドでエラーコード接尾辞を導入する
   からです (`v>0xff;2`)。
3. **より厳しい起動条件。** パターンファイルでは `!` はパターン変数のシジル (`ADD A,!d`)
   であり、ほぼすべての行に現れるので、その行が本当にマクロ文として取られる場合、
   エスケープされていない `!{...}` を含む場合、または `}` で始まる場合にのみ層が起動
   します。マクロを使わないパターンファイルは層を完全にスキップします。同梱のすべての
   パターンファイルはバイト単位で自分自身に展開され、従来と同じ時間でアセンブルされます。

生成されたパターンテキストをアセンブルせずに確認するには `-p` を使います。

### 7.5 互換性と制限

後方互換性:

- `!` で始まる行が横取りされるのは、キーワードを含む場合、定義済みマクロを名指しする
  場合、または直後に `(` が続く場合だけです。VLIW の `!!` と `!F` / `!D` / `!Q` 形式は
  そのままです。
- 行頭の `}` がブロックを閉じるのは、ブロックが開いているときだけです。
- マクロを含まないソースは、マクロ層が存在する前と同一の出力を生成します。

制限:

- ソース側の `.include` はマクロ層をバイパスします。マクロ定義ファイルには `!include`
  を使ってください。（パターン側の `.include` は層を通り*ます*。）
- `!{a ? b : c:04x}` — 三項演算子とフォーマット指定の組み合わせ — は現在パースできません。
- プロンプトモードはマクロ層をバイパスします。

暴走防止: 再帰 200 段、`!while` 反復 1,000,000 回、生成行 2,000,000 行、`!include` の
入れ子深さ 64。いずれかの上限を超えるとエラーを発生させ、そのパスの残りの展開を中断
します。

### 7.6 例

```asm
!include "lib.inc"

!def table(name, from, to) {
!{name}:
!set v = from
!while v <= to {
DB !{v}
!set v = v + 1
}
}

!def loopblk(n) {
!if n == 0 !then {
!return
}
L!{__id__}_top:
LD A,!{n}
LD HL,L!{__id__}_top
}

!def sq(x) {
!return x * x
}

start:
LD HL,end
!table("mytab", 1, 5)
!loopblk(2)
LD HL,!{sq(16)}
end:
NOP
```

---

## 8. オブジェクト出力、エクスポートとインポート

### 8.1 ファイル形式

`-e`、`-E`、`-i` が扱うファイルは**タブ区切り**です。フィールドは本物のタブで区切られて
いなければならず、スペースで区切られた行は黙って無視されます。

レコードの形は 2 種類あります。セクションレコードは 3 フィールド（`-E` では 4）、
ラベルレコードは 2 フィールドです:

```
セクション名   開始アドレス   サイズ   [フラグ]
ラベル名       値
```

### 8.2 エクスポート (`-e`, `-E`)

アドレス、サイズ、値は `0x` 接頭辞付きで書かれます。`-E` はセクションレコードに
ELF セクションフラグ (`AX`、`WA`、…) を保持する第 4 フィールドを追加します。`-e` は
それを省きます。ラベルは `.export` / `.global` が宣言した順に現れます。

`axx x86_64.axx hello.s -E hello.tsv`:

```
.text	0x401000	0x39	AX
_hello	0x401000
_start	0x401000
len	0xd
```

### 8.3 インポート (`-i`)

インポートファイルは同じ 2 つのレコード形を使い、混在させられます。
**値は `0x` 接頭辞なしの 16 進として読まれます** — これはエクスポート形式と異なります。

- 3 フィールドの行はセクションのアドレス範囲を宣言します。
- 2 フィールドの行はインポートされるラベルを定義します。そのセクションは、アドレスが
  どの宣言済み範囲に入るかを調べて推定されます。どれにも一致しなければ `.text` が
  仮定されます。

インポートされるラベルには `::` で再配置タイプを付けられます:

```
.text	401000	39
mylabel	401010
otherlabel::pc32	401020
```

`::` の後に受け付けられる名前は、`ELF_MACHINES` で選択されたマシンの `named` テーブルに
ある短縮名です。x86-64 の場合: `abs64`、`abs32`、`abs32s`、`abs16`、`abs8`、`pc32`、
`rel32`、`plt32`、`pc16`、`pc8`、`pc64`、`got32`、`gotpcrel`、`got64`。認識されない名前
は警告を出して無視されます。

同じ名前をパターンファイルの `.reloc`（3.7.5 節）も取ります。AArch64 の `call26` の
ような命令フィールド型はそちらでしか意味を持ちません。シンボル単位では、どのオペランド
位置に当てるのかを言えないからです。

セクションレコードは省略可能です。ラベル値だけが必要なら:

```
label1	0
label2	1
label3	2
```

---

## 9. エラー

アセンブラ自身が発生させる診断:

| 条件 | メッセージ |
|---|---|
| ラベルがパターンファイルのシンボルと衝突 | *is a pattern file symbol* |
| ラベルが複数回定義されている | *label already defined* |
| 行をパースできない | *Syntax error*（`.passthru` が有効なときは素通しされる。3.16 節） |
| 参照されたラベルが定義されていない | *Label undefined* |
| アセンブラ行またはパターン行の形式が不正 | *Illegal syntax in assembler line or pattern line* |
| EPIC テンプレートが設定されていない | *No VLIW instruction-set defined* |
| VLIW パターンファイルの形式が不正 | 解釈中に報告される |

`error_patterns` が発生させ、`;` の後のコードで選ばれるエラー:

| コード | メッセージ |
|---|---|
| 1 | Invalid syntax. |
| 2 | Address out of range. |
| 3 | Value out of range. |
| 4 | *(なし)* |
| 5 | Register out of range. |
| 6 | Port number out of range. |
| 7 以上 | *(なし)* |

本文のないコードも依然としてエラーを発生させ、出力ファイルの書き出しを妨げます。空白に
なるのはメッセージだけです。どちらの実装のソースにも手を触れずにパターンファイルから
メッセージを追加・上書きするには、`.error::n::"メッセージ"`（3.14 節）を使います。

axx ミニ言語の `.raise n` ディレクティブは、エラーコード n のエラーを発生させます。

---

## 10. 設計ノートと背景

*この節は背景です。axx を使うのにここにあるものは何も必要ありません。*

### 10.1 由来

`axx` は "Arbitrary eXtended X(cross) assembler" の略です。名前はまた、X — 未知の CPU —
を "ASM" に重ね合わせたことにも由来します。リファレンス実装が Python なので、その愛称は
Paxx です。

中心となるアイデア、名前、そして C によるプロトタイプは 1986 年に存在しており、大学時代
に東京電子設計でアルバイトをしながら着想されました。元のリストが 38 年後に再発見され、
今日公開されている動作コードは、それを 2024 年に Python で書き直したものです。

### 10.2 メタ言語

`instruction` フィールドは手続き型アセンブリ言語のためのメタ言語です。固定の文法を持た
ない DSL — 文字列リテラル、シンボル、式から自分の文法を組み立てる自由構文のパターン言語
です。

最小限まで還元すると、手続き型アセンブリ言語は `instruction :: binary_list` です。
エラーチェックは追加であり、axx の `binary_list` が式の評価、アラインメント、`;` 修飾子
を加えているのは必要性からではなく実用のためです。

axx がしているのは、ノイマン型アーキテクチャの共通構造を抽出し、ISA をメタモデル化し、
その結果をパターンマッチングとして形式化することです。

### 10.3 なぜパターン言語はチューリング完全でないのか

プロセッサアーキテクチャは、そうしようと選べばいくらでも複雑にできます。チューリング
完全なパターン言語はそれにどこまでも追随できますが、axx のものはできません。それこそが
axx を万能 (universal) ではなく汎用 (general) アセンブラたらしめているものです。

この制限の理由は、チューリング完全な DSL はパターンファイルを*プログラム*にしてしまい、
パターンマッチングの停止性が保証されなくなるからです。その保証は、追加の射程よりも
価値があると判断されました。マクロ層は別の段階であり、このような制限を受けません。

パターンファイルはチューリング完全ではありませんが、計算が必要な場合は `binary_list`
から `.call func()` でチューリング完全な関数（ミニ言語）を呼び出せます。

パターンファイルは明示的な構造を持たないので、構造化されていない命令エンコーディングに
よく適合します。`.check`（3.6 節）は、望むときに構造を課すための手段です — たとえば
`a`、`b`、`c` がそれぞれ固定の集合を動く `MOVabc r,s` のような命令です。

---

## 付録 A. 例

### A.1 Z80

```
.setsym:: BC:: 0x00
.setsym:: DE:: 0x10
.setsym:: HL:: 0x20
LD s,!d:: (s&0xf!=0)||(s>>4)>3;9 :: s|0x01,d&0xff,d>>8
```

`ld bc,0x1234`、`ld de,0x1234`、`ld hl,0x1234` はそれぞれ `0x01,0x34,0x12`、
`0x11,0x34,0x12`、`0x21,0x34,0x12` を出力します。

### A.2 複数プロセッサの断片

テストフィクスチャです。エンコーディングは本物ではありません。

```test.axx
/* test
.setsym ::a:: 7
.setsym ::b:: 1
.setsym ::%% ::7
.setsym ::||:: 8
LDF A,!Fx :: 0x1,x,*(x,1),*(x,2),*(x,3)
LDD A,!Dx :: 0x1,@@[8,*(x,%%)]
LDQ A,!Qx :: 0x1,@@[16,*(x,%%)]
LDR A,[ [ !x ] ]:: ~~0x3?3:0,x,x>>8
LD\! A,B::0xcd

/* ARM64
.setsym ::r1 :: 2
.setsym ::r2 :: 3
.setsym ::r3 :: 4
.setsym ::lsl:: 6
VMOV.F32 S!n,#!Fd::0x80|n,d>>24,d>>16,d>>8,d
ADD w, x, y z #!d :: 0x88,d
.check ::q::r1,r2
ADD q, y, !e :: 0x91,q,y,e
.clrcheck::q

/* A64FX
.setsym ::v0 :: 0
.setsym ::x0 :: 1
ST1 {x.4S},\[y\] :: 0x01,x,y,0

/* MIPS
.setsym ::$s5 ::21
.setsym ::$v0 ::2
.setsym ::$a0 ::4
ADDI x,y,!d :: @@[4,*(e:=(0x20000000|(y<<21)|(x<<16)|d&0xffff),(3-%%))]

/* x86_64
.setsym ::rax:: 0
.setsym ::rbx:: 3
.setsym ::rcx ::1
.setsym ::rep ::1
.setsym ::per::2
.clearsym::per

MMX A,B ::  ,0x12,0x13
LEAQ r,\[s,t,!d,!e\] :: 0x48,0x8d,0x04,((@d)-1)<<6|t<<3|s,e
LEAQ r, ( s+t*!h\+!i) :: 0x48,0x8d,0x04,((@h)-1)<<6|t<<3|s,i
[[z]]MOVSB :: ;z?0xf3:0,0xa4
TEST !a:: a==3?0xc0:4,0x12,0x13

/* ookakko test
LD (IX[[+!d]]),(IX[[+!e]]):: 0xfd,0x04,d,e 
NOP :: 0x01
LOAD A,[B] :: 0x43
REPEAT !n::@@[n,%%],%0@@[n,0x10+%%]
```

```test.s
ldf a,3.14
ldf a,3.14*2+1
ldd a,3.14
ldd a,-inf
ldq a,3.14*2+1
leaq rax , [ rbx , rcx , 2 , 0x40]
leaq rax , ( rbx + rcx * (2+2) + 0x40 )
addi $v0,$a0,5
vmov.f32 s0,#3.14
st1 {v0.4s},[x0]
add r1, r2, r3 lsl #20
rep movsb
movsb
load a,[b]
repeat 10
ldf a,label
label: .equ flt{3.14}
ldf a,flt{enfloat(:label)*2}
```

```
$ axx test.axx test.s -v
0000000000000000 test.s 1 ldf a,3.14  0x01 0xc3 0xf5 0x48 0x40
0000000000000005 test.s 2 ldf a,3.14*2+1  0x01 0xc3 0xf5 0xe8 0x40
000000000000000a test.s 3 ldd a,3.14  0x01 0x1f 0x85 0xeb 0x51 0xb8 0x1e 0x09 0x40
0000000000000013 test.s 4 ldd a,-inf  0x01 0x00 0x00 0x00 0x00 0x00 0x00 0xf0 0xff
000000000000001c test.s 5 ldq a,3.14*2+1  0x01 0x1f 0x85 0xeb 0x51 0xb8 0x1e 0x85 0xeb 0x51 0xb8 0x1e 0x85 0xeb 0xd1 0x01 0x40
000000000000002d test.s 6 leaq rax , [ rbx , rcx , 2 , 0x40]  0x48 0x8d 0x04 0x4b 0x40
0000000000000032 test.s 7 leaq rax , ( rbx + rcx * (2+2) + 0x40 )  0x48 0x8d 0x04 0x8b 0x40
0000000000000037 test.s 8 addi $v0,$a0,5  0x20 0x82 0x00 0x05
000000000000003b test.s 9 vmov.f32 s0,#3.14  0x80 0x40 0x48 0xf5 0xc3
0000000000000040 test.s 10 st1 {v0.4s},[x0]  0x01 0x00 0x01 0x00
0000000000000044 test.s 11 add r1, r2, r3 lsl #20  0x88 0x14
0000000000000046 test.s 12 rep movsb  0xf3 0xa4
0000000000000048 test.s 13 movsb  0xa4
0000000000000049 test.s 14 load a,[b]  0x43
000000000000004a test.s 15 repeat 10  0x00 0x01 0x02 0x03 0x04 0x05 0x06 0x07 0x08 0x09 0x10 0x11 0x12 0x13 0x14 0x15 0x16 0x17 0x18 0x19
000000000000005e test.s 16 ldf a,label  0x01 0xec 0x91 0x80 0x4e
0000000000000063 test.s 17 label: .equ flt{3.14} 
0000000000000063 test.s 18 ldf a,flt{enfloat(:label)*2}  0x01 0xec 0x91 0x81 0x4e
```

### A.3 AArch64 論理即値

おそらく単一のパターンで表現できる最も複雑なものです。このようなエンコーディングは
1 つのマクロにまとめられます（7.4 節）。

同梱の `aarch64_logical_mini.axx` は同じ命令グループを、代わりに
[3.15 節](#315-ミニ言語-func--call)のミニ言語でカバーしています。自分でこのような
ものを書くなら、そちらから始めるのがよい形式です。下の 1 行版は、宣言的コアの射程を
示すデモンストレーションとしてここに残してあります。

```
AND d,n,#!v ::v==0;3,v==0xFFFFFFFFFFFFFFFF;3 ::;(e:=((v&3)*0x5555555555555555==v)?2:((v&0xf)*0x1111111111111111==v)?4:((v&0xff)*0x0101010101010101==v)?8:((v&0xffff)*0x1000100010001==v)?16:((v&0xffffffff)*0x100000001==v)?32:64)*0,;(m:=(1<<e)-1)*0,;(y:=v&m)*0,;(t:=@(y^(y-1))-1)*0,;(u:=y>>t)*0,;(w:=(y^m)==0?1:y^m)*0,;(p:=@(w^(w-1))-1)*0,;(q:=w>>p)*0,;(c:=((u+1)&u)==0)*0,;(b:=c?@u:e-@q)*0,
 ;(r:=c?(e-t)&(e-1):(e-(p+@q))&(e-1))*0,;(s:=((-2*e)&0x7f)|(b-1))*0,;(z:=(1<<31)|(0x24<<23)|((((s>>6)&1)^1)<<22)|(r<<16)|((s&0x3f)<<10)|(n<<5)|d)*0,@@[4,z>>(%%*8)]
```

---

## 付録 B. 同梱パターンファイル

`x86_64.axx`、`x86_64m.axx`、`68000.axx`、`z80.axx`、`8080.axx`、`8048.axx`、
`8051.axx`、`6502.axx`、`6800.axx`、`6809.axx`、`4004.axx` は実用向けです。
`aarch64_logical_mini.axx` も、カバーする 1 つの命令グループの範囲内では実用向けです。
残りはテストフィクスチャです。

x86_64 パターンファイルは
<https://github.com/fygar256/x86_64_pattern_file_for_axx> でも別途メンテナンスされて
います。

| パターンファイル | サイズ | `::` 行数 | ソース | 備考 |
|---|---|---|---|---|
| **x86_64.axx** | 3.9 MB | 23,923 | **hello.s** | x86_64-v3: セグメントアドレッシング、AVX/AVX2、BMI1/BMI2、x87、EVEX/AVX-512 |
| **x86_64m.axx** | 935 KB | 5,787 | **hello.s** | マクロで書かれた x86_64-v3。Brainfuck デモでも使用 |
| **aarch64_logical_mini.axx** | 9.2 KB | 86 | **aarch64_logical_mini_demo.s** | AArch64 論理（即値）: AND/ORR/EOR/ANDS/TST、32 ビットと 64 ビット。ビットマスク即値をミニ言語（3.15 節）でエンコード |
| **6809.axx** | 124 KB | 1,950 | **6809.s** | Motorola 6809 |
| **68000.axx** | 51 KB | 453 | **68000.s** | Motorola 68000 |
| **6800.axx** | 18 KB | 271 | **6800.s** | Motorola 6800 |
| **6502.axx** | 14 KB | 192 | **6502.s** | MOS 6502 |
| **z80.axx** | 7.5 KB | 283 | **z80.s** | Zilog Z80 |
| **8051.axx** | 8.9 KB | 111 | **8051.s** | Intel 8051 |
| **8080.axx** | 6.0 KB | 113 | **8080.s** | Intel 8080 |
| **8048.axx** | 6.3 KB | 95 | **8048.s** | Intel 8048 |
| **4004.axx** | 5.4 KB | 53 | **4004.s** | Intel 4004 |
| **test.axx** | 1.1 KB | 40 | **test.s** | 複数 ISA の断片。テスト専用 |
| **itanium.axx** | 281 B | 12 | **vliw.s** | Itanium (EPIC) のスケッチ。未完成 |
| **vliw.axx** | 178 B | 10 | **vliw.s** | 非 EPIC VLIW。テスト専用 |
| **bf.axx** | 128 B | 9 | **bf.s** | Brainfuck 仮想 CPU。hello-world デモ。同梱だが `test1` の対象外 |

`x86_64.axx` が対になるのは `hello.s` であって、`x86_64.s` という名前のファイルでは
ないことに注意してください。`itanium.axx` も `vliw.s` を使い、
`aarch64_logical_mini.axx` は `aarch64_logical_mini_demo.s` と対になります。

`test1` は上記のうち 14 組を両方の実装で実行し、結果を比較します。`bf.axx` / `bf.s` は
同梱の対のうち唯一カバーされていないものです。

現在実装されているものの大半は x86_64 とレガシー CPU ですが、これは作業が向けられた
先を反映しているだけで、axx が記述できる範囲の限界ではありません。

---

## 付録 C. 関連資料

### C.1 このリポジトリ内の文書

| ファイル | 内容 |
|---|---|
| `MACRO.md` / `macro_en.md` | マクロ層リファレンス（日本語 / 英語）。7 節と同じ内容 |
| `MINI.md` / `mini_en.md` | ミニ言語リファレンス（日本語 / 英語）。3.15 節と同じ内容 |
| `axx_introduction_paper.md` / `_en.md` | 紹介論文: 自由構文パターン言語の設計根拠、特異度スコア、宣言的コアからの計算能力の分離 |
| `axxsemantics` | axx の表示的意味論による形式化。ラベル環境上の不動点として読むリラクゼーションを含む。そこにある数式はその文書独自の構成であり、公式仕様ではない |
| `FILE_DESCRIPTION` | 全ファイルの 1 行説明 |
| `format_of_exp_imp_file` | エクスポート/インポートファイル形式 |
| `axx.1.gz` | man ページ |

`test1` は同梱の 14 組のパターン/ソースの対を両方の実装でアセンブルし、結果を比較します。

### C.2 外部

**テスト環境:** FreeBSD、Linux ターミナル。

- 元記事（日本語）: <https://qiita.com/fygar256/items/1d06fb757ac422796e31>
- 再配置可能 ELF の生成: <https://github.com/fygar256/axx_relocatable_elf_generation>
- Brainfuck インタプリタのデモンストレーション:
  <https://github.com/fygar256/brainfuck_interpreter_for_axx_on_freebsd_of_x86_64>

---

## 付録 D. ロードマップ

### D.1 未実装

パターンファイルに加えて高度な構造化マクロと最適化があれば、これはかなり高性能な
システムになるでしょうが、構造化アセンブリのマクロ構成要素の全範囲をカバーするのは
一人の手に余ります。引き受けてくださる方がいれば、喜んで拝見します。

### D.2 axx2 構想

パターンファイル用のより記述的なメタ言語は、可読性を高め、評価順序への依存をなくし、
制御文を書きやすくし、プロセッサ記述ファイルをデバッグしやすくするでしょう。パターン
データのほうが直感的なので、これは純粋な利得ではなくトレードオフです。

さらに一般化して — 記述的メタ言語、現在の `binary_list` における文字列リテラルと文字列
操作、加えて制御文 — があれば、中間言語の生成とアセンブリ言語間の変換が可能になります。
`binary_list` は `object_list` になり、パターンファイルはパターンデータではなく複数行の
言語で記述される*プロセッサ仕様ファイル*になるでしょう。これは実現可能で、どうやら
axx をもとに取り組んでいる方がいるようです。

現在のパターンファイルでも、コマンド文字列を変数に代入して — `a='MOV b,c'` — それを
`binary_list` で参照することでマクロが書けます。1 文字の小文字変数を完全なシンボルに
拡張し、展開用の `expand(a)` を追加し（`a='b ; c'`、`b='MOV AX,d'`、`c='JMPC e'` で
`'MOV AX,d ; JMPC e'` が得られる）、評価用の `expression(a)` と定義用の `label:` を
加えれば、かなりのところまで行けるでしょう。

axx 自体の中にループ構造があると無限ループのデバッグが難しくなります。評価をパターン
ファイルに閉じ込めておけば、自己参照チェックとともに、ループと分岐を許しつつデバッグを
扱いやすく保てます。チューリング完全性はあらゆるプロセッサアーキテクチャを可能にする
でしょう — 原理的には LISP マシンも含めて。パターンファイルのラベルをアセンブリ
ファイルのものと分離しておけば、同じラベルが両方に現れる心配はなくなります。EPIC 流の
メタ処理は変数の列挙で解決されます。

その代償は大幅な書き直しであり、より複雑なプロセッサ記述ファイルは汎用逆アセンブラ
との互換性を難しくします。

---

## 付録 E. プロジェクトノート

### E.1 ノート

- 慣例的でない記法についてはご容赦ください。
- axx は量子コンピュータと LISP マシンをサポートしません。量子コンピュータが実行するの
  は量子アセンブリであり、LISP マシンが実行するのはそもそもアセンブリ言語ではありません。
- 自作プロセッサからスーパーコンピュータまで、どうぞご自由にお使いください。
- axx を評価し、拡張し、改変してください。構造は複雑ですが Python なので、拡張は容易
  です。
- 定数は現在 4 倍精度に制限されており、これは Python 3 の制限です。Python 4 が 4 倍
  精度をネイティブに扱ってくれるとよいのですが。
- マクロ層は組み込みですが、あらゆるアセンブリ言語をカバーするにはより強力なマクロ
  プロセッサ — 関数型・構造化アセンブリの構成要素を手続き型に落とすもの — が必要です。
- アセンブラはもともと機械語を人間に読めるようにするために作られました。AI がコードを
  書くようになった今、アセンブリ言語とマシンの両方をカバーする汎用アセンブラは持つ価値
  があるように思えます — そして大きな ISA 用のパターンファイルの生成は、まさに AI が
  やるべき種類の仕事です。最近の AI はそれができます。

### E.2 バグ報告

バグを見つけたら、何が動かないか教えてください。
