# axx — 1986年に生まれ、38年間眠っていたアセンブラ

## 30秒で分かること

axx は、**どんな命令セットのアセンブラも、1個の宣言的なテキストファイルだけで作れる**ことを目指したツールです。

```
RET :: 0xc3
```

これで x86_64 の `RET` 命令1個分のアセンブラが完成します。C++もSchemeも要りません。この一行と同じ形式（`命令の書き方 :: エラー条件 :: 出力バイト列`）を並べていくだけで、Intel 4004 からx86_64のAVX-512まで、実在するCPUのアセンブラを作れます。

- GitHub: https://github.com/fygar256/axx
- 作者: fygar256（前川田井介）
- ライセンス: MIT

## なぜこれを作ったか

axxのアイデアと名前、そしてCで書かれた試作コードは、作者が大学生だった1986年、東京電子設計でのアルバイト中に生まれました。それから38年、原本のリストが偶然発掘され、2024年にPythonで書き直されて初めて世に出ました。

この空白の38年は、単なる放置期間ではありません。VLIW、EPIC、ワード幅が8ビットでないプロセッサ——その間に登場したハードウェアの多様化を経ても、このアイデアの骨格が通用し続けるかどうかの検証期間でもありました。実際、axxはそれら全てに対応しています。

## 何が実際に検証できているか

宣伝文句ではなく、実際に手を動かして確かめられることだけを書きます。

**2つの独立実装がバイト単位で一致する。** axxにはPython実装（`axx.py`、通称Paxx、9,789行）とC実装（`caxx.c`、通称Caxx、13,538行）があります。同梱の`test1`スクリプトは、4004からx86_64、Brainfuck仮想CPUまで15種類のパターンファイルを両実装でアセンブルし、`cmp`でバイト単位比較します。実行すると`test all passed`——本当に一致します。これは主張ではなく、誰でもクローンして5分で再現できる事実です。

**実際にELFオブジェクトが生成できる。**

```sh
axx x86_64.axx hello.s -o out.o
file out.o
# => out.o: ELF 64-bit LSB relocatable, x86-64, version 1 (SYSV), not stripped
```

FreeBSDとLinux向けに、ELF32/64のリロケータブルオブジェクトを`ld`にそのまま渡せる形で出力します。DWARFデバッグ情報の埋め込みにも対応しています。同じ土俵の類似ツール(後述)には無い機能です。

**文法そのものが自由。** axxにはトークナイザがありません。1文字単位でマッチングするので、`MOV A,B`のような伝統的な「ニーモニック＋オペランド」形式だけでなく、`r1 = r2 + r3`のような代入式スタイルの命令も、特別扱いなしで普通のパターンとして書けます。これは伊達ではなく、LLVMのアセンブラ生成機構(TableGen/AsmMatcher)が、ニーモニックを持たないHexagonの`r0 = r1`という構文のために専用のパッチを必要とした、という実例と対照的です。axxは最初からこの制約を持っていません。

**マクロ層による保守性。** 同じ命令を並べて手で書く必要はありません。x86_64のフルセット(AVX-512/EVEXまで含む)は、生の形だと23,923パターン行ですが、マクロを使った版は5,787行——4分の1です。展開後の実行速度も、C実装で0.2秒程度と実用上問題ありません。

## 対象アーキテクチャ

同梱済みで実際に動くもの: **x86_64**(x86_64-v3、AVX/AVX2、BMI1/BMI2、x87、EVEX/AVX-512)、**Motorola 6809 / 68000 / 6800**、**MOS 6502**、**Zilog Z80**、**Intel 8080 / 8051 / 8048 / 4004**。

ARM・AArch64・RISC-V・PowerPC・MIPS・SPARCは、まだパターンファイルがありません。これは設計上の限界ではなく、実機やエミュレータでの検証環境が今のところ用意できておらず、作者一人では手が回っていないというだけの、労力上の制約です。パターンファイルの文法自体はREADMEに詳細な仕様があり、Turing不完全性という制約はあるものの「命令とエンコードが一対一対応するプロセッサ」であれば原理的に記述できます。

## 似たツールとの違い

**customasm**(Rust製、活発に開発中)は、axxと同じく宣言的パターンファイルでアセンブラを作る発想を共有していて、`#subruledef`による部分文法の合成など、構造化の面ではaxxより進んだ機能を持っています。ただし出力形式はROM・エミュレータ向けのダンプ形式(binary、hexdump、intelhexなど)に限られ、ELFのようなリンク可能なオブジェクト出力はありません。「自作の仮想CPU・FPGA実装で遊ぶ」用途にはcustomasmが、「実OS上でリンクして動くバイナリを作る」用途にはaxxが向いています。

**LLVM MC**は、clangやrustcが実際に使っている本番品質のツールチェーンで、対応アーキテクチャの広さもオブジェクト出力の完成度(ELF/COFF/Mach-O/wasm)も別次元です。ただし宣言的な記述(TableGen)だけでは完結せず、多くの場合数千行規模の手書きC++が必要です。「今すぐ本番で使いたい主要アーキテクチャ」ならLLVM、「歴史的CPUや変わったISAを、一人が読める分量の宣言だけで動かしたい」ならaxx、という住み分けです。

**CGEN**はSchemeで書く命令記述言語で、GNU binutils/GDBの一部の組み込み系アーキテクチャ移植に使われていますが、命令の意味論(RTL)を記述する言語であり、axxのような表層テキストのパターンマッチとは発想が異なります。

## こんな人に向いています

- 自分の好きな古いCPU(あるいは自作CPU)向けに、本物のELFオブジェクトを吐けるアセンブラが欲しい人
- OS自作・ブートローダー自作で、変わったターゲット向けのツールチェーンを組みたい人
- 「アセンブラの本質とは何か」という設計そのものに興味がある人
- 大規模ISAのパターンファイルを書く作業に、AIによる支援を活用したい人(宣言的で機械的な形式なので相性が良い)

## 試してみる

```sh
git clone https://github.com/fygar256/axx.git
cd axx
make                              # caxx・paxx・axx・man pageをビルド/インストール
axx z80.axx z80.s -v              # Z80のサンプルをアセンブルしてリスト表示
axx x86_64.axx hello.s -o out.o   # x86_64のhello worldをELFオブジェクトに
```

ARM・RISC-V・PowerPC・MIPS・SPARC向けのパターンファイルは、まだ存在しません。実機・エミュレータでの検証も含めて、一人でやり切るには荷が重い作業です。興味のある方の力を借りられたら、と思っています。

---

*このドキュメントに書かれた検証結果(2実装のバイト一致、ELFオブジェクトの実生成、行数比較など)は、いずれも実際にリポジトリをクローンし、ビルドし、実行した上で確認したものです。*
